// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths, operations } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import {
  PaginatedAsyncGenerator,
  DeepReadonly,
  HydratePage,
} from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link DataModelsResource["list"] | list}. */
export interface ListDataModelsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  /** Whether to exclude additional information about tagged versions of each result */
  excludeTags?: boolean;
  /** Only available to Sigma admins. Return all data models, regardless of whether the user has access. */
  skipPermissionCheck?: boolean;
}

/** Query parameters for {@link DataModelsResource["get"] | get}. */
export interface GetDataModelQuery {
  /** Whether to exclude additional information about tagged versions of each result */
  excludeTags?: boolean;
}

/** Query parameters for {@link DataModelsResource["listColumns"] | listColumns}. */
export interface ListDataModelColumnsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

/** Query parameters for {@link DataModelsResource["listElements"] | listElements}. */
export interface ListDataModelElementsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  tagName?: string;
}

/** Query parameters for {@link DataModelsResource["listLineageTree"] | listLineageTree}. */
export interface ListDataModelLineageTreeQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

/** Query parameters for {@link DataModelsResource["listMaterializationSchedules"] | listMaterializationSchedules}. */
export interface ListDataModelMaterializationSchedulesQuery {
  /**
   * Use to specify the next set of results with the string returned in the `nextPageToken` field of the previous response.
   */
  pageToken?: string;
  /** Number of results to return per page, with a maximum of 500. Defaults to 50. */
  pageSize?: number;
}

/** Query parameters for {@link DataModelsResource["listSources"] | listSources}. */
export interface ListDataModelSourcesQuery {
  /**
   * Use to specify the next set of results with the string returned in the `nextPageToken` field of the previous response.
   */
  pageToken?: string;
  /** Number of results to return per page, with a maximum of 500. Defaults to 50. */
  pageSize?: number;
}

/** Query parameters for {@link DataModelsResource["getSpec"] | getSpec}. */
export interface GetDataModelSpecQuery {
  documentVersion?: number;
}

/** Query parameters for {@link DataModelsResource["listTags"] | listTags}. */
export interface ListDataModelTagsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link DataModelsResource["materializeElement"] | materializeElement}. */
export interface MaterializeDataModelElementBody {
  /** Identifier for the materialization schedule of this element. */
  sheetId: string;
}

/** Request body for {@link DataModelsResource["updateSpec"] | updateSpec}. */
export type UpdateDataModelSpecBody = NonNullable<
  operations['updateDataModelSpec']['requestBody']
>['content']['application/json'];

/** Request body for {@link DataModelsResource["swapSources"] | swapSources}. */
export type SwapDataModelSourcesBody = NonNullable<
  operations['swapDataModelSources']['requestBody']
>['content']['application/json'];

/** Request body for {@link DataModelsResource["createSpec"] | createSpec}. */
export type CreateDataModelSpecBody = NonNullable<
  operations['createDataModelSpec']['requestBody']
>['content']['application/json'];

/** Request body for {@link DataModelsResource["tag"] | tag}. */
export interface TagDataModelBody {
  /** Unique identifier of the data model. */
  dataModelId: string;
  /** Name of the version tag to apply. */
  tag: string;
  /** Specific version of the published data model to tag. */
  dataModelVersion?: number;
  /** Swap sources for the data model when you add a tag. */
  sourceMappingConfig?: {
    fromConnectionId: string;
    toConnectionId: string;
    paths: {
      fromPath: string[];
      toPath: string[];
    }[];
  }[];
  /** Which tagged version of the data model to use as the source. */
  dataModelSourceTaggedVersions?: {
    dataModelId: string;
    fromVersionTagId: string | null;
    toVersionTagId: string | null;
  }[];
  /**
   * Whether to make this tag the default tagged version to load if the user doesn't have access to the published version.
   */
  isDefault?: boolean;
}

/** Request body for {@link DataModelsResource["sourceSwapV3Alpha"] | sourceSwapV3Alpha}. */
export type V3alpha_sourceSwapDataModelBody = NonNullable<
  operations['v3alpha_sourceSwapDataModel']['requestBody']
>['content']['application/json'];

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

/** Response from {@link DataModelsResource["list"] | list}. */
export interface ListDataModelsResponse {
  /** Array of results returned by the endpoint */
  entries: {
    dataModelId: string;
    dataModelUrlId: string;
    name: string;
    url: string;
    path: string;
    latestVersion: number;
    ownerId: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
    isArchived?: boolean;
    tags?: {
      versionTagId: string;
      tagName: string;
      sourceVersion: number;
      taggedAt: string;
    }[];
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

/** Response from {@link DataModelsResource["get"] | get}. */
export interface GetDataModelResponse {
  /** Unique identifier of the data model. */
  dataModelId: string;
  dataModelUrlId: string;
  name: string;
  url: string;
  path: string;
  latestVersion: number;
  ownerId: string;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  isArchived?: boolean;
  tags?: {
    versionTagId: string;
    tagName: string;
    sourceVersion: number;
    taggedAt: string;
  }[];
}

/** Response from {@link DataModelsResource["materializeElement"] | materializeElement}. */
export interface MaterializeDataModelElementResponse {
  materializationId: string;
}

/** Response from {@link DataModelsResource["listColumns"] | listColumns}. */
export interface ListDataModelColumnsResponse {
  entries: {
    columnId: string;
    name: string;
    formula: string;
    type: Record<string, unknown>;
    elementId: string;
  }[];
  nextPage: string | null;
  total?: number;
}

/** Response from {@link DataModelsResource["listElements"] | listElements}. */
export interface ListDataModelElementsResponse {
  entries: {
    elementId: string;
    name?: string;
    type?: string;
    columns?: string[];
    vizualizationType?: string;
    error?: string;
  }[];
  nextPage: string | null;
  total?: number;
}

/** Response from {@link DataModelsResource["listLineageTree"] | listLineageTree}. */
export interface ListDataModelLineageTreeResponse {
  entries: (
    | {
        type: 'table' | 'dataset' | 'data-model' | 'element' | 'customSQL';
        sourceIds: string[] | null;
        elementId: string;
        dataSourceIds: string[] | null;
      }
    | {
        type: 'table' | 'dataset' | 'data-model' | 'element' | 'customSQL';
        connectionId: string | null;
        name: string | null;
        dataModelId: string;
      }
    | {
        type: 'table' | 'dataset' | 'data-model' | 'element' | 'customSQL';
        connectionId: string | null;
        name: string | null;
        inodeId: string;
      }
    | {
        type: 'table' | 'dataset' | 'data-model' | 'element' | 'customSQL';
        connectionId: string | null;
        name: string | null;
        definition: string;
      }
  )[];
  nextPage: string | null;
  total?: number;
}

/** Response from {@link DataModelsResource["getMaterialization"] | getMaterialization}. */
export interface GetDataModelMaterializationResponse {
  materializationId: string;
  status: string;
  startedAt?: string;
  readyAt?: string;
  expiredAt?: string;
  error?: string;
}

/** Response from {@link DataModelsResource["listMaterializationSchedules"] | listMaterializationSchedules}. */
export interface ListDataModelMaterializationSchedulesResponse {
  /** Array of results returned by the endpoint */
  entries: {
    sheetId: string;
    elementName: string;
    schedule: {
      cronSpec?: string;
      timezone?: string;
    } | null;
    configuredAt: string;
    paused: boolean;
  }[];
  /**
   * A string that can be passed to the `pageToken` parameter in the next request to fetch the next page of results. Not present in the last page of results.
   * **Must be treated as an opaque string.**
   */
  nextPageToken?: string;
}

/** Response from {@link DataModelsResource["listSources"] | listSources}. */
export interface ListDataModelSourcesResponse {
  /** Array of results returned by the endpoint */
  entries: (
    | {
        type: 'data-model';
        dataModelId: string;
        elementIds: string[];
        versionTagId?: string;
      }
    | {
        type: 'dataset';
        datasetId: string;
      }
    | {
        type: 'table';
        tableId: string;
      }
    | {
        type: 'custom-sql';
        connectionId: string;
        definition: string;
        customSqlId: string;
      }
  )[];
  /**
   * A string that can be passed to the `pageToken` parameter in the next request to fetch the next page of results. Not present in the last page of results.
   * **Must be treated as an opaque string.**
   */
  nextPageToken?: string;
}

/** Response from {@link DataModelsResource["getSpec"] | getSpec}. */
export interface GetDataModelSpecResponse {
  /** Unique identifier of the data model. */
  dataModelId: string;
  /** The name of the data model. */
  name: string;
  /** The full URL for the requested document version of the data model. */
  url: string;
  /** The document version of the data model. */
  documentVersion: number;
  /** The most recently published document version of the data model. */
  latestDocumentVersion: number;
  /** The identifier of the user who owns the data model. */
  ownerId: string;
  /** The identifier of the folder in which the data model is located. */
  folderId: string;
  /** The identifier of the user who created the data model. */
  createdBy: string;
  /** The identifier of the user or process that last updated the data model. */
  updatedBy: string;
  /** When the data model was created. */
  createdAt: string;
  /** When the data model was last updated. */
  updatedAt: string;
  /** The description of the data model. */
  description?: string;
  /** The schema version used by this representation of the data model. */
  schemaVersion: 1;
  /** The pages of the data model and their contents. */
  pages: {
    id: string;
    name: string;
    elements: (
      | {
          id: string;
          kind: 'table';
          columns: {
            id: string;
            formula: string;
            name?: string;
            description?: string;
            hidden?: boolean;
            format?:
              | {
                  kind: 'number';
                  formatString?: string;
                  prefix?: string;
                  suffix?: string;
                  displayNullAs?: string;
                  decimalSymbol?: string;
                  digitGroupingSymbol?: string;
                  digitGroupingSize?: number[];
                  currencySymbol?: string;
                }
              | {
                  kind: 'datetime';
                  formatString?: string;
                };
          }[];
          source:
            | {
                connectionId: string;
                kind: 'warehouse-table';
                path: string[];
              }
            | {
                connectionId: string;
                kind: 'sql';
                statement: string;
              }
            | {
                kind: 'table';
                elementId: string;
                groupingId?: string;
              }
            | {
                dataModelId: string;
                elementId: string;
                kind: 'data-model';
                groupingId?: string;
              }
            | {
                kind: 'join';
                joins: {
                  left:
                    | {
                        connectionId: string;
                        kind: 'warehouse-table';
                        path: string[];
                      }
                    | {
                        kind: 'table';
                        elementId: string;
                        groupingId?: string;
                      }
                    | {
                        dataModelId: string;
                        elementId: string;
                        kind: 'data-model';
                        groupingId?: string;
                      };
                  right:
                    | {
                        connectionId: string;
                        kind: 'warehouse-table';
                        path: string[];
                      }
                    | {
                        kind: 'table';
                        elementId: string;
                        groupingId?: string;
                      }
                    | {
                        dataModelId: string;
                        elementId: string;
                        kind: 'data-model';
                        groupingId?: string;
                      };
                  columns: {
                    left: string;
                    right: string;
                    op?:
                      | '<'
                      | '<='
                      | '='
                      | '!='
                      | '>='
                      | '>'
                      | 'within'
                      | 'intersects'
                      | 'is-distinct-from'
                      | 'is-not-distinct-from';
                  }[];
                  name?: string;
                  joinType?:
                    | 'inner'
                    | 'left-outer'
                    | 'right-outer'
                    | 'full-outer'
                    | 'lookup';
                }[];
                name?: string;
                primarySource?:
                  | {
                      connectionId: string;
                      kind: 'warehouse-table';
                      path: string[];
                    }
                  | {
                      kind: 'table';
                      elementId: string;
                      groupingId?: string;
                    }
                  | {
                      dataModelId: string;
                      elementId: string;
                      kind: 'data-model';
                      groupingId?: string;
                    };
              }
            | {
                kind: 'union';
                sources: (
                  | {
                      connectionId: string;
                      kind: 'warehouse-table';
                      path: string[];
                    }
                  | {
                      kind: 'table';
                      elementId: string;
                      groupingId?: string;
                    }
                  | {
                      dataModelId: string;
                      elementId: string;
                      kind: 'data-model';
                      groupingId?: string;
                    }
                )[];
                matches: {
                  outputColumnName: string;
                  sourceColumns: (string | null)[];
                }[];
              }
            | ({
                kind: 'transpose';
                source:
                  | {
                      connectionId: string;
                      kind: 'warehouse-table';
                      path: string[];
                    }
                  | {
                      kind: 'table';
                      elementId: string;
                      groupingId?: string;
                    }
                  | {
                      dataModelId: string;
                      elementId: string;
                      kind: 'data-model';
                      groupingId?: string;
                    };
              } & (
                | {
                    direction: 'row-to-column';
                    outputColumns: string[];
                    columnToTranspose: string;
                    valueColumn: string;
                    aggregate:
                      | 'min'
                      | 'max'
                      | 'count'
                      | 'count-if'
                      | 'count-distinct'
                      | 'sum'
                      | 'avg'
                      | 'median';
                  }
                | {
                    direction: 'column-to-row';
                    columnsToMerge: string[];
                    columnLabelForMergedColumns: string;
                    columnLabelForValues: string;
                  }
              ));
          name?: string;
          description?: string;
          metrics?: {
            id: string;
            formula: string;
            name?: string;
            description?: string;
            isHighlighted?: boolean;
            format?:
              | {
                  kind: 'number';
                  formatString?: string;
                  prefix?: string;
                  suffix?: string;
                  displayNullAs?: string;
                  decimalSymbol?: string;
                  digitGroupingSymbol?: string;
                  digitGroupingSize?: number[];
                  currencySymbol?: string;
                }
              | {
                  kind: 'datetime';
                  formatString?: string;
                };
            timeline?: {
              dateColumnId: string;
              truncation?:
                | 'year'
                | 'quarter'
                | 'month'
                | 'week'
                | 'day'
                | 'hour'
                | 'minute'
                | 'second';
              comparison?: {
                comparisonPeriod:
                  | 'year'
                  | 'quarter'
                  | 'month'
                  | 'week'
                  | 'day'
                  | 'hour'
                  | 'minute'
                  | 'second';
                direction?: 'higher-is-better' | 'lower-is-better';
              };
            };
          }[];
          relationships?: {
            id: string;
            targetElementId: string;
            keys: {
              sourceColumnId: string;
              targetColumnId: string;
            }[];
            name?: string;
            description?: string;
          }[];
          filters?: ({
            id: string;
            columnId: string;
          } & {
            state?: 'enabled' | 'disabled';
          } & (
              | {
                  kind: 'number-range';
                  includeNulls?:
                    'always' | 'never' | 'when-no-value-is-selected';
                  min?: number;
                  max?: number;
                }
              | ({
                  kind: 'date-range';
                } & (
                  | {
                      mode: 'between';
                      startDate: string;
                      endDate: string;
                    }
                  | {
                      mode: 'between';
                    }
                  | {
                      mode: 'on';
                      date: string;
                    }
                  | {
                      mode: 'before';
                      date: string;
                    }
                  | {
                      mode: 'after';
                      date: string;
                    }
                  | {
                      mode: 'next';
                      value: number;
                      unit:
                        | 'year'
                        | 'quarter'
                        | 'month'
                        | 'week-starting-sunday'
                        | 'week-starting-monday'
                        | 'day'
                        | 'hour'
                        | 'minute';
                      includeToday: boolean;
                    }
                  | {
                      mode: 'last';
                      value: number;
                      unit:
                        | 'year'
                        | 'quarter'
                        | 'month'
                        | 'week-starting-sunday'
                        | 'week-starting-monday'
                        | 'day'
                        | 'hour'
                        | 'minute';
                      includeToday: boolean;
                    }
                  | {
                      mode: 'current';
                      unit:
                        | 'year'
                        | 'quarter'
                        | 'month'
                        | 'week-starting-sunday'
                        | 'week-starting-monday'
                        | 'day'
                        | 'hour'
                        | 'minute';
                    }
                  | {
                      mode: 'custom';
                      startDate:
                        | string
                        | {
                            op: 'now-minus' | 'now-plus';
                            unit:
                              | 'year'
                              | 'quarter'
                              | 'month'
                              | 'week-starting-sunday'
                              | 'week-starting-monday'
                              | 'day'
                              | 'hour'
                              | 'minute';
                            value: number;
                          };
                      endDate:
                        | string
                        | {
                            op: 'now-minus' | 'now-plus';
                            unit:
                              | 'year'
                              | 'quarter'
                              | 'month'
                              | 'week-starting-sunday'
                              | 'week-starting-monday'
                              | 'day'
                              | 'hour'
                              | 'minute';
                            value: number;
                          };
                    }
                  | {
                      mode: 'custom';
                      startDate: {
                        op: 'now-minus' | 'now-plus';
                        unit:
                          | 'year'
                          | 'quarter'
                          | 'month'
                          | 'week-starting-sunday'
                          | 'week-starting-monday'
                          | 'day'
                          | 'hour'
                          | 'minute';
                        value: number;
                      };
                    }
                  | {
                      mode: 'custom';
                      endDate: {
                        op: 'now-minus' | 'now-plus';
                        unit:
                          | 'year'
                          | 'quarter'
                          | 'month'
                          | 'week-starting-sunday'
                          | 'week-starting-monday'
                          | 'day'
                          | 'hour'
                          | 'minute';
                        value: number;
                      };
                    }
                ) & {
                    includeNulls?:
                      'always' | 'never' | 'when-no-value-is-selected';
                  })
              | ({
                  kind: 'top-n';
                } & (
                  | {
                      rankingFunction: 'rank' | 'rank-dense' | 'row-number';
                      mode: 'top-n' | 'bottom-n';
                      rowCount?: number;
                      includeNulls?:
                        'always' | 'never' | 'when-no-value-is-selected';
                    }
                  | {
                      rankingFunction: 'rank-percentile' | 'cume-dist';
                      mode: 'top-percentile' | 'bottom-percentile';
                      percentile?: number;
                      includeNulls?:
                        'always' | 'never' | 'when-no-value-is-selected';
                    }
                ))
              | {
                  kind: 'list';
                  mode?: 'include' | 'exclude';
                  values?:
                    | (null | boolean)[]
                    | (null | number)[]
                    | (null | string)[]
                    | (null | string)[];
                }
              | {
                  kind: 'text-match';
                  mode:
                    | 'equals'
                    | 'does-not-equal'
                    | 'contains'
                    | 'does-not-contain'
                    | 'starts-with'
                    | 'does-not-start-with'
                    | 'ends-with'
                    | 'does-not-end-with'
                    | 'like'
                    | 'not-like'
                    | 'matches-regexp'
                    | 'does-not-match-regexp';
                  value?: string;
                  case?: 'sensitive' | 'insensitive';
                  includeNulls?:
                    'always' | 'never' | 'when-no-value-is-selected';
                }
            ))[];
          folders?: {
            id: string;
            name: string;
            items?: string[];
          }[];
          groupings?: {
            id: string;
            groupBy?: string[];
            calculations?: string[];
          }[];
          summary?: string[];
          order?: string[];
          visibleAsSource?: boolean;
          sort?: {
            columnId: string;
            direction: 'ascending' | 'descending';
            nulls?: 'first' | 'last' | 'connection-default';
          }[];
          columnSecurities?: {
            id: string;
            restrictedColumns: string[];
            criteria:
              | {
                  kind: 'no-one-can-view';
                }
              | {
                  kind: 'specific-users-and-teams';
                  assignments: (
                    | {
                        type: 'user';
                        userId: string;
                      }
                    | {
                        type: 'team';
                        teamId: string;
                      }
                  )[];
                }
              | {
                  kind: 'user-attribute';
                  assignments: {
                    userAttributeId: string;
                    value: string;
                  }[];
                };
          }[];
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'checkbox';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode: 'True/False' | 'True/All';
          value?: boolean;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'switch';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode: 'True/False' | 'True/All';
          value?: boolean;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'text';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode:
            | 'equals'
            | 'does-not-equal'
            | 'contains'
            | 'does-not-contain'
            | 'starts-with'
            | 'does-not-start-with'
            | 'ends-with'
            | 'does-not-end-with'
            | 'like'
            | 'not-like'
            | 'matches-regexp'
            | 'does-not-match-regexp';
          case?: 'sensitive' | 'insensitive';
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
          showOperators?: boolean;
          value?: string;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'text-area';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          value?: string;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'number';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode: '<=' | '=' | '>=';
          value?: number;
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'number-range';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
          min?: number;
          max?: number;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'date';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode: '<=' | '=' | '>=';
          value?:
            | string
            | {
                op: 'now-minus' | 'now-plus';
                unit:
                  | 'year'
                  | 'quarter'
                  | 'month'
                  | 'week-starting-sunday'
                  | 'week-starting-monday'
                  | 'day'
                  | 'hour'
                  | 'minute';
                value: number;
              };
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
        }
      | ({
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'date-range';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
        } & {
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
        } & (
            | {
                mode: 'between';
                startDate: string;
                endDate: string;
              }
            | {
                mode: 'between';
              }
            | {
                mode: 'on';
                date: string;
              }
            | {
                mode: 'before';
                date: string;
              }
            | {
                mode: 'after';
                date: string;
              }
            | {
                mode: 'next';
                value: number;
                unit:
                  | 'year'
                  | 'quarter'
                  | 'month'
                  | 'week-starting-sunday'
                  | 'week-starting-monday'
                  | 'day'
                  | 'hour'
                  | 'minute';
                includeToday: boolean;
              }
            | {
                mode: 'last';
                value: number;
                unit:
                  | 'year'
                  | 'quarter'
                  | 'month'
                  | 'week-starting-sunday'
                  | 'week-starting-monday'
                  | 'day'
                  | 'hour'
                  | 'minute';
                includeToday: boolean;
              }
            | {
                mode: 'current';
                unit:
                  | 'year'
                  | 'quarter'
                  | 'month'
                  | 'week-starting-sunday'
                  | 'week-starting-monday'
                  | 'day'
                  | 'hour'
                  | 'minute';
              }
            | {
                mode: 'custom';
                startDate:
                  | string
                  | {
                      op: 'now-minus' | 'now-plus';
                      unit:
                        | 'year'
                        | 'quarter'
                        | 'month'
                        | 'week-starting-sunday'
                        | 'week-starting-monday'
                        | 'day'
                        | 'hour'
                        | 'minute';
                      value: number;
                    };
                endDate:
                  | string
                  | {
                      op: 'now-minus' | 'now-plus';
                      unit:
                        | 'year'
                        | 'quarter'
                        | 'month'
                        | 'week-starting-sunday'
                        | 'week-starting-monday'
                        | 'day'
                        | 'hour'
                        | 'minute';
                      value: number;
                    };
              }
            | {
                mode: 'custom';
                startDate: {
                  op: 'now-minus' | 'now-plus';
                  unit:
                    | 'year'
                    | 'quarter'
                    | 'month'
                    | 'week-starting-sunday'
                    | 'week-starting-monday'
                    | 'day'
                    | 'hour'
                    | 'minute';
                  value: number;
                };
              }
            | {
                mode: 'custom';
                endDate: {
                  op: 'now-minus' | 'now-plus';
                  unit:
                    | 'year'
                    | 'quarter'
                    | 'month'
                    | 'week-starting-sunday'
                    | 'week-starting-monday'
                    | 'day'
                    | 'hour'
                    | 'minute';
                  value: number;
                };
              }
          ))
      | ({
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'top-n';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
        } & (
          | {
              rankingFunction: 'rank' | 'rank-dense' | 'row-number';
              mode: 'top-n' | 'bottom-n';
              rowCount?: number;
              includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
            }
          | {
              rankingFunction: 'rank-percentile' | 'cume-dist';
              mode: 'top-percentile' | 'bottom-percentile';
              percentile?: number;
              includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
            }
        ))
      | ({
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'list';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode?: 'include' | 'exclude';
        } & {
          source: {
            kind: 'source';
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
            displayColumnId?: string;
          };
        } & (
            | {
                selectionMode: 'single';
                value?: null | boolean | number | string | string;
              }
            | {
                selectionMode?: 'multiple';
                values?:
                  | (null | boolean)[]
                  | (null | number)[]
                  | (null | string)[]
                  | (null | string)[];
              }
          ))
      | ({
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'list';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode?: 'include' | 'exclude';
        } & {
          source?: {
            kind: 'manual';
            valueType: 'text' | 'number' | 'date' | 'boolean';
            values?: boolean[] | number[] | string[] | string[];
            labels?: (string | null)[];
          };
        } & (
            | {
                selectionMode: 'single';
                value?: null | boolean | number | string | string;
              }
            | {
                selectionMode?: 'multiple';
                values?:
                  | (null | boolean)[]
                  | (null | number)[]
                  | (null | string)[]
                  | (null | string)[];
              }
          ))
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'segmented';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          clearLabel?: string;
          showClearLabel?: boolean;
          source?:
            | {
                kind: 'manual';
                valueType: 'text' | 'number' | 'date' | 'boolean';
                values?: boolean[] | number[] | string[] | string[];
                labels?: (string | null)[];
              }
            | {
                kind: 'source';
                source:
                  | {
                      kind: 'table';
                      elementId: string;
                    }
                  | {
                      connectionId: string;
                      kind: 'warehouse-table';
                      path: string[];
                    }
                  | {
                      dataModelId: string;
                      elementId: string;
                      kind: 'data-model';
                    };
                columnId: string;
                displayColumnId?: string;
              };
          value?: null | boolean | number | string | string;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'hierarchy';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          source?: {
            source: {
              kind: 'table';
              elementId: string;
            };
            columnId: string;
          };
          mode?: 'include' | 'exclude';
          values?: string[][];
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'slider';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          mode: '<=' | '=' | '>=';
          low?: number;
          high?: number;
          step?: number;
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
          value?: number;
        }
      | {
          kind: 'control';
          id: string;
          controlId: string;
          controlType: 'range-slider';
          filters?: {
            source:
              | {
                  kind: 'table';
                  elementId: string;
                }
              | {
                  connectionId: string;
                  kind: 'warehouse-table';
                  path: string[];
                }
              | {
                  dataModelId: string;
                  elementId: string;
                  kind: 'data-model';
                };
            columnId: string;
          }[];
          parameters?: {
            kind: 'data-model';
            dataModelId: string;
            controlId: string;
          }[];
          low?: number;
          high?: number;
          step?: number;
          includeNulls?: 'always' | 'never' | 'when-no-value-is-selected';
          min?: number;
          max?: number;
        }
    )[];
  }[];
}

/** Response from {@link DataModelsResource["updateSpec"] | updateSpec}. */
export interface UpdateDataModelSpecResponse {
  success: boolean;
  dataModelId: string;
}

/** Response from {@link DataModelsResource["swapSources"] | swapSources}. */
export interface SwapDataModelSourcesResponse {
  /** IDs of sources in the workbook that were not swapped. */
  unswappedSources: string[];
  /** IDs of custom SQL connections in the workbook that were not swapped. */
  unswappedConnections: string[];
}

/** Response from {@link DataModelsResource["listTags"] | listTags}. */
export interface ListDataModelTagsResponse {
  entries: {
    versionTagId: string;
    tagName: string;
    sourceVersion: number;
    taggedAt: string;
  }[];
  nextPage: string | null;
  total?: number;
}

/** Response from {@link DataModelsResource["createSpec"] | createSpec}. */
export interface CreateDataModelSpecResponse {
  success: boolean;
  dataModelId: string;
}

/** Response from {@link DataModelsResource["tag"] | tag}. */
export interface TagDataModelResponse {
  versionTagId: string;
  /** Unique identifier of the data model. */
  taggedDataModelId: string;
  taggedDataModelVersion: number;
}

/** Response from {@link DataModelsResource["sourceSwapV3Alpha"] | sourceSwapV3Alpha}. */
export type V3alpha_sourceSwapDataModelResponse = Record<string, never>;

// ---------------------------------------------------------------------------
// Sub-resource accessors (hydration)
// ---------------------------------------------------------------------------

/** Sub-resource accessors available on hydrated datamodel entries. */
export interface DataModelSubResources {
  columns: {
    /**
     * List columns for all elements in a data model
     *
     * Source docs: https://help.sigmacomputing.com/reference/getdatamodelcolumns
     *
     * This endpoint retrieves all columns in a data model for all elements in the data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *   - **Data exploration:** Enable users to explore the data structure of a data model element, facilitating deeper analysis and understanding.
     *   - **Integration tasks:** Useful for developers integrating Sigma with other tools that need to know the data structure to map data accurately.
     *
     *   ### Best practices
     *   - Use this endpoint when setting up interfaces that require specific knowledge about the data structure.
     *
     * @param query - Query parameters
     * @returns ListDataModelColumnsResponse
     */
    list(
      query?: ListDataModelColumnsQuery,
    ): Promise<DeepReadonly<ListDataModelColumnsResponse>>;
    /**
     * List columns for all elements in a data model (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/getdatamodelcolumns
     *
     * This endpoint retrieves all columns in a data model for all elements in the data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *   - **Data exploration:** Enable users to explore the data structure of a data model element, facilitating deeper analysis and understanding.
     *   - **Integration tasks:** Useful for developers integrating Sigma with other tools that need to know the data structure to map data accurately.
     *
     *   ### Best practices
     *   - Use this endpoint when setting up interfaces that require specific knowledge about the data structure.
     *
     * @param query - Query parameters
     * @yields ListDataModelColumnsResponse
     */
    listAll(
      query?: ListDataModelColumnsQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelColumnsResponse>>;
  };
  elements: {
    /**
     * Run a scheduled materialization for a data model element
     *
     * Source docs: https://help.sigmacomputing.com/reference/materializedatamodelelement
     *
     * This endpoint runs a scheduled materialization for an element in a data model. Materialization processes the data of the specified element, allowing the data to be stored or cached for optimized access and performance.
     *
     *   For more details on materialization, see [Materialization](https://help.sigmacomputing.com/docs/materialization).
     *
     *   ### Usage notes
     *   - The materialization schedule for the specified element must be created beforehand.
     *   - Retrieve the **sheetId** by calling the [/v2/dataModels/{dataModelId}/materialization-schedules](listdatamodelmaterializationschedules) endpoint.
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *   - **Performance optimization:** Use this endpoint to improve response times for frequently accessed data model elements.
     *   - **Data refresh:** Allows users to manually (programmatically) refresh the data of specific data model elements to ensure that the latest data is available for analysis and reporting.
     *
     *   ### Best practices
     *   - Prioritize materialization for elements that are heavily used or form critical components of business reports.
     *   - Monitor the performance impacts of materialization and adjust strategies as necessary to optimize resource usage and response times.
     *
     * @param body - The request body.
     * @returns MaterializeDataModelElementResponse
     */
    materialize(
      body: MaterializeDataModelElementBody,
    ): Promise<DeepReadonly<MaterializeDataModelElementResponse>>;
    /**
     * List elements in a data model
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelelements
     *
     * This endpoint retrieves details of all elements in a data model, which helps you understand and interact with the individual components that make up the data model, such as tables or controls.
     * ### Usage notes
     * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     * - To retrieve elements from version-tagged data models, pass the `tagName` in the query parameters with the tag name. Retrieve the tag name by calling the [/v2/dataModels/{dataModelId}/tags](listdatamodeltags) endpoint and using the `tags.name` included in the response for a given data model.
     * ### Usage scenarios
     * - **Detailed analysis:** Developers and users can retrieve specific elements for detailed data analysis or further manipulation.
     * - **Interface customization:** Allows for dynamic interface construction where users can choose which elements to display or interact with.
     * ### Best practices
     * - Use pagination to handle large numbers of elements efficiently, reducing load times and improving user experience.
     *
     * @param query - Query parameters
     * @returns ListDataModelElementsResponse
     */
    list(
      query?: ListDataModelElementsQuery,
    ): Promise<DeepReadonly<ListDataModelElementsResponse>>;
    /**
     * List elements in a data model (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelelements
     *
     * This endpoint retrieves details of all elements in a data model, which helps you understand and interact with the individual components that make up the data model, such as tables or controls.
     * ### Usage notes
     * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     * - To retrieve elements from version-tagged data models, pass the `tagName` in the query parameters with the tag name. Retrieve the tag name by calling the [/v2/dataModels/{dataModelId}/tags](listdatamodeltags) endpoint and using the `tags.name` included in the response for a given data model.
     * ### Usage scenarios
     * - **Detailed analysis:** Developers and users can retrieve specific elements for detailed data analysis or further manipulation.
     * - **Interface customization:** Allows for dynamic interface construction where users can choose which elements to display or interact with.
     * ### Best practices
     * - Use pagination to handle large numbers of elements efficiently, reducing load times and improving user experience.
     *
     * @param query - Query parameters
     * @yields ListDataModelElementsResponse
     */
    listAll(
      query?: ListDataModelElementsQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelElementsResponse>>;
  };
  lineageTrees: {
    /**
     * List lineage for a data model
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodellineagetree
     *
     * This endpoint lists the elements, data sources, and upstream lineage for those data sources in a data model, providing a detailed view of how data is interconnected within a data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *   - **Data governance:** Allows data managers to track how information is used and propagated through different data model elements.
     *   - **Debugging and optimization:** Helps with identifying and resolving issues in data processing or data model structure.
     *   - **Data source management**: Identify the dependencies of data sources in use in a specific data model.
     *
     *   ### Best practices
     *   - Integrate this information into documentation or data dictionaries to enhance transparency and understanding across teams.
     *   - Use this endpoint to regularly audit data model structures, ensuring that all data connections and transformations are correct and optimized.
     *
     * @param query - Query parameters
     * @returns ListDataModelLineageTreeResponse
     */
    list(
      query?: ListDataModelLineageTreeQuery,
    ): Promise<DeepReadonly<ListDataModelLineageTreeResponse>>;
    /**
     * List lineage for a data model (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodellineagetree
     *
     * This endpoint lists the elements, data sources, and upstream lineage for those data sources in a data model, providing a detailed view of how data is interconnected within a data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *   - **Data governance:** Allows data managers to track how information is used and propagated through different data model elements.
     *   - **Debugging and optimization:** Helps with identifying and resolving issues in data processing or data model structure.
     *   - **Data source management**: Identify the dependencies of data sources in use in a specific data model.
     *
     *   ### Best practices
     *   - Integrate this information into documentation or data dictionaries to enhance transparency and understanding across teams.
     *   - Use this endpoint to regularly audit data model structures, ensuring that all data connections and transformations are correct and optimized.
     *
     * @param query - Query parameters
     * @yields ListDataModelLineageTreeResponse
     */
    listAll(
      query?: ListDataModelLineageTreeQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelLineageTreeResponse>>;
  };
  materializationSchedules: {
    /**
     * List materialization schedules for a data model
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelmaterializationschedules
     *
     *
     *   This endpoint retrieves a list of all materialization schedules associated with a data model. Each schedule details when and how often specific data model elements are automatically materialized to optimize performance and data freshness.
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *   - To retrieve additional pages of results, use the response of the `nextPageToken` parameter as the `pageToken` query parameter in the following request.
     *
     *   ### Usage scenarios
     *   - **Monitoring and management:** Administrators can monitor and manage the schedules for materialization to ensure optimal system performance.
     *   - **Audit and review:** Periodic reviews of materialization schedules can help in assessing the efficiency of data processes and making necessary adjustments.
     *
     *   ### Best practices
     *   - Regularly review materialization schedules to align them with current data usage patterns and business needs.
     *   - Use pagination to manage large sets of data and improve response times of API calls.
     *
     * @param query - Query parameters
     * @returns ListDataModelMaterializationSchedulesResponse
     */
    list(
      query?: ListDataModelMaterializationSchedulesQuery,
    ): Promise<DeepReadonly<ListDataModelMaterializationSchedulesResponse>>;
    /**
     * List materialization schedules for a data model (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelmaterializationschedules
     *
     *
     *   This endpoint retrieves a list of all materialization schedules associated with a data model. Each schedule details when and how often specific data model elements are automatically materialized to optimize performance and data freshness.
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *   - To retrieve additional pages of results, use the response of the `nextPageToken` parameter as the `pageToken` query parameter in the following request.
     *
     *   ### Usage scenarios
     *   - **Monitoring and management:** Administrators can monitor and manage the schedules for materialization to ensure optimal system performance.
     *   - **Audit and review:** Periodic reviews of materialization schedules can help in assessing the efficiency of data processes and making necessary adjustments.
     *
     *   ### Best practices
     *   - Regularly review materialization schedules to align them with current data usage patterns and business needs.
     *   - Use pagination to manage large sets of data and improve response times of API calls.
     *
     * @param query - Query parameters
     * @yields ListDataModelMaterializationSchedulesResponse
     */
    listAll(
      query?: ListDataModelMaterializationSchedulesQuery,
    ): PaginatedAsyncGenerator<
      DeepReadonly<ListDataModelMaterializationSchedulesResponse>
    >;
  };
  sources: {
    /**
     * List data model sources
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelsources
     *
     * List data sources of a data model given the dataModelId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     * @param query - Query parameters
     * @returns ListDataModelSourcesResponse
     */
    list(
      query?: ListDataModelSourcesQuery,
    ): Promise<DeepReadonly<ListDataModelSourcesResponse>>;
    /**
     * List data model sources (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodelsources
     *
     * List data sources of a data model given the dataModelId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     * @param query - Query parameters
     * @yields ListDataModelSourcesResponse
     */
    listAll(
      query?: ListDataModelSourcesQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelSourcesResponse>>;
  };
  tagsResource: {
    /**
     * List tags for a data model
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodeltags
     *
     * Retrieve the version tag information for a specific data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *
     *   - Provide on-demand performant access to version tag details for a data model. For example, make a GET request to the [/v2/dataModels](listdatamodels) endpoint with the `excludeTags` parameter set to `True` to improve performance, then use this endpoint to return tag information for specific data models.
     *
     * @param query - Query parameters
     * @returns ListDataModelTagsResponse
     */
    list(
      query?: ListDataModelTagsQuery,
    ): Promise<DeepReadonly<ListDataModelTagsResponse>>;
    /**
     * List tags for a data model (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listdatamodeltags
     *
     * Retrieve the version tag information for a specific data model.
     *
     *   ### Usage notes
     *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
     *
     *   ### Usage scenarios
     *
     *   - Provide on-demand performant access to version tag details for a data model. For example, make a GET request to the [/v2/dataModels](listdatamodels) endpoint with the `excludeTags` parameter set to `True` to improve performance, then use this endpoint to return tag information for specific data models.
     *
     * @param query - Query parameters
     * @yields ListDataModelTagsResponse
     */
    listAll(
      query?: ListDataModelTagsQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelTagsResponse>>;
  };
  /**
   * Get the code representation of a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelspec
   *
   * This endpoint returns a code representation of an existing data model, including metadata and contents. The response can be formatted as either JSON or YAML, and contains a data model's pages, elements, columns, sources, and more. The representation can be used to store, copy, or modify the data model programmatically.
   *
   * For more information on managing data models via the Sigma API, see [Manage data models as code](https://help.sigmacomputing.com/docs/manage-data-models-as-code). For more information on using this endpoint, including an end-to-end example, see [Get the code representation of a data model](https://help.sigmacomputing.com/docs/get-the-code-representation-of-a-data-model).
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user with **Can view** access to the data model.
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint. Invalid or nonexistent IDs return an error.
   * - The response includes the data sources, metadata, and relationships necessary to recreate the data model. The data itself is not included in the response.
   * - To create a new data model from code, use the [/v2/dataModels/spec](createdatamodelspec) endpoint.
   * - To update an existing data model from code, use the [/v2/dataModels/{dataModelId}/spec](updatedatamodelspec) endpoint.
   * - The default format of the representation is JSON. To receive YAML, add the query parameter `?format=yaml` or the header `Accept: application/yaml`.
   *
   * ### Usage scenarios
   * - **Backup and restore**: Developers can use this endpoint to programmatically back up data models and restore them if needed.
   * - **Version control**: Developers can use this endpoint to programmatically manage, update, and version control data models between several organizations or tenants.
   * - **Administration**: Administrators can use this endpoint to programmatically analyze and govern data models across an organization.
   * - **Migration**: Administrators can use this endpoint to programmatically migrate data models between organizations or tenants.
   * - **Agentic workflows**: Agents can use these endpoints to directly manage data model contents in code.
   *
   * @param query - Query parameters
   * @returns GetDataModelSpecResponse
   */
  getSpec(
    query?: GetDataModelSpecQuery,
  ): Promise<DeepReadonly<GetDataModelSpecResponse>>;
  /**
   * Update a data model from a code representation
   *
   * Source docs: https://help.sigmacomputing.com/reference/updatedatamodelspec
   *
   * This endpoint replaces a data model with the contents defined in a code representation. You can programmatically update the data model's structure, metadata, and contents. This creates a new version of the data model, which is accessible from the Sigma UI.
   *
   * For more information on managing data models via the Sigma API, see [Manage data models as code](https://help.sigmacomputing.com/docs/manage-data-models-as-code). For more information on using this endpoint, including an end-to-end example, see [Update a data model from a code representation](https://help.sigmacomputing.com/docs/update-a-data-model-from-a-code-representation).
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned an account type with **Create, edit, and publish data models** permission.
   * - To perform this operation, you must use API credentials owned by a user with **Can edit** access to the data model.
   * - Retrieve a representation of an existing data model by calling the [/v2/dataModels/{dataModelId}/spec](getdatamodelspec) endpoint.
   * - To create a new data model from code, use the [/v2/dataModels/spec](createdatamodelspec) endpoint.
   * - The endpoint only supports updates to the `pages` array and its contents. Additional fields are not required and are ignored in the request body.
   * - Partial updates are not supported. You must provide the entire representation of the data model in the request body.
   * - The default format of the representation is JSON. To use YAML, add the header `Content-Type: application/yaml`.
   *
   * ### Usage scenarios
   * - **Automation**: If you want to update several data models across one or more organizations, or in response to external events, you can use this endpoint to programmatically update the data models.
   * - **Integration**: Using this endpoint, you can update data models based on external data sources or integrate Sigma with other tools and platforms.
   * - **Version control**: Developers can use this endpoint to programmatically manage, update, and version control data models between several organizations or tenants.
   * - **Agentic workflows**: Agents can use these endpoints to directly manage data model contents in code.
   *
   * @param body - The request body.
   * @returns UpdateDataModelSpecResponse
   */
  updateSpec(
    body: UpdateDataModelSpecBody,
  ): Promise<DeepReadonly<UpdateDataModelSpecResponse>>;
  /**
   * Swap data model sources
   *
   * Source docs: https://help.sigmacomputing.com/reference/swapdatamodelsources
   *
   * Swap the data sources used by a data model. You can swap from one connection to another, and/or from one dataset or table to another.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *   - Retrieve the **connectionId** by calling the [/v2/connections](listconnections) endpoint.
   *   - To map the sources of a specific element, you can use the table or dataset ID:
   *
   *     - List sources of data model by calling the [/v2/dataModels/{dataModelId}/sources](listdatamodelsources) endpoint.
   *     - Retrieve table and dataset IDs by calling the [/v2/files](fileslist) endpoint. Use the `typeFilters` option to filter for table and dataset files. Use `id` field in the response as the `fromId` or `toId`, as relevant.
   *
   *   - To swap all tables in a connection, set the `swapTables` option to `true`.
   *
   *     - Use the `paths` option to specify the connection paths to swap. The connection path is a list of strings that represents the path to the table in the cloud data warehouse. For example, `["database", "schema", "table"]`.
   *     - If the table schema is the same in both connections, leave the `paths` option empty to swap all tables in the connection. You can also pass a partial path to swap between databases, catalogs, or schemas.
   *     - If `sourceMapping` is provided, those mappings take precedence over the connection swaps.
   *     - If `swapTables` is not set to `true`, only Custom SQL elements have their connections changed.
   *
   *   ### Usage scenarios
   *   - **Data model development**: Use a test data source while developing a data model, then swap the source to a production database when ready to share the data model for production use.
   *
   * @param body - The request body.
   * @returns SwapDataModelSourcesResponse
   */
  swapSources(
    body: SwapDataModelSourcesBody,
  ): Promise<DeepReadonly<SwapDataModelSourcesResponse>>;
  /**
   * Swap data model sources (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/v3alpha_sourceswapdatamodel
   *
   * Swap each data source used by a data model. You can swap from one table, dataset, data model element, or custom SQL element to another for each data source.
   *
   * **Beta**: This documentation describes a private beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   * - Retrieve data model sources by calling the [/v2/dataModels/{dataModelId}/sources](listdatamodelsources) endpoint.
   * - Retrieve the **datasetId** by calling the [/v2/datasets](listdatasets) endpoint.
   * - Retrieve the **tableId** by calling the [/v2/connection](lookupconnection) endpoint.
   * - Retrieve the **connectionId** for a new custom SQL element by calling the [/v2/connections](listconnections) endpoint.
   *
   * - Use the `sourceMapping` options in the request body to swap specific sources. For each source in the source mapping, provide the current source in use in the **from** object, and provide the source that you want to use instead in the **to** object.
   * - If your source is a data model with one or more tagged versions, you can also provide a `versionTagId` to swap to or from a tagged version of the data model. The data model version must already be tagged.
   * - When swapping **from** a custom SQL element, identify it by its element ID using the `customSqlId` field. When swapping **to** a custom SQL element, supply the `connectionId` of the connection to run against and the SQL query in the `definition` field.
   * - If your source is a data model or a dataset, you can use the `metricMapping` option to map metrics that rely on column names from the old source to the new one so that they continue to work.
   * - If the source that you swap from uses column names, links, or relationships different from the source that you are swapping to, use the `columnMapping` option to map each column in the original source to a corresponding column in the new source.
   * - When `columnMapping` is omitted, columns are automatically matched by display name, ignoring differences in case and punctuation. For example, a custom SQL output column named `DOCK_COUNT` automatically maps to a column with display name `Dock Count` in the target source. Provide `columnMapping` explicitly to override the automatic match for a column.
   *
   * ### Usage scenarios
   * - **Data model development**: Use a test data source during development, then swap to a production source when ready.
   * - **Dataset migration**: Update references to legacy dataset sources with data model sources by mapping dataset IDs to their new data model equivalents.
   *
   * @param body - The request body.
   * @returns V3alpha_sourceSwapDataModelResponse
   */
  sourceSwapV3Alpha(
    body: V3alpha_sourceSwapDataModelBody,
  ): Promise<DeepReadonly<V3alpha_sourceSwapDataModelResponse>>;
  /**
   * Get a materialization job
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelmaterialization
   *
   * This endpoint retrieves details of a specific materialization job. It provides comprehensive information including status, start and completion times, which helps for monitoring and troubleshooting materialization.
   *
   * ### Usage notes
   * - Retrieve the **materializationId** by calling the [/v2/dataModels/materializations](materializedatamodelelement) endpoint.
   *
   * ### Usage scenarios
   * - **Job Monitoring:** Enables administrators to track the progress and status of materialization jobs, aiding in operational oversight.
   * - **Troubleshooting:** Provides detailed information needed to diagnose and resolve issues in materialization processes.
   *
   * ### Best practices
   * - Monitor materialization jobs regularly to detect and resolve issues promptly.
   * - Use the detailed information provided by this endpoint to optimize materialization settings and performance.
   *
   * @param materializationId - Unique identifier of the materialization job
   * @returns GetDataModelMaterializationResponse
   */
  getMaterialization(
    materializationId: string,
  ): Promise<DeepReadonly<GetDataModelMaterializationResponse>>;
}

/** A datamodel entry with sub-resource accessor methods. Response data properties are deeply readonly. */
export type HydratedDataModel<T> = DeepReadonly<T> & DataModelSubResources;

// ---------------------------------------------------------------------------
// DataModelsResource
// ---------------------------------------------------------------------------

/** @internal Attach sub-resource accessors to a datamodel entry. */
function _hydrateDataModel<T extends object>(
  entry: T,
  resource: DataModelsResource,
): HydratedDataModel<T> {
  return Object.assign(entry, {
    columns: {
      list: (query?: ListDataModelColumnsQuery) =>
        resource.listColumns((entry as any).dataModelId, query),
      listAll: (query?: ListDataModelColumnsQuery) =>
        resource.listColumnsAll((entry as any).dataModelId, query),
    },
    elements: {
      materialize: (body: MaterializeDataModelElementBody) =>
        resource.materializeElement((entry as any).dataModelId, body),
      list: (query?: ListDataModelElementsQuery) =>
        resource.listElements((entry as any).dataModelId, query),
      listAll: (query?: ListDataModelElementsQuery) =>
        resource.listElementsAll((entry as any).dataModelId, query),
    },
    lineageTrees: {
      list: (query?: ListDataModelLineageTreeQuery) =>
        resource.listLineageTree((entry as any).dataModelId, query),
      listAll: (query?: ListDataModelLineageTreeQuery) =>
        resource.listLineageTreeAll((entry as any).dataModelId, query),
    },
    materializationSchedules: {
      list: (query?: ListDataModelMaterializationSchedulesQuery) =>
        resource.listMaterializationSchedules(
          (entry as any).dataModelId,
          query,
        ),
      listAll: (query?: ListDataModelMaterializationSchedulesQuery) =>
        resource.listMaterializationSchedulesAll(
          (entry as any).dataModelId,
          query,
        ),
    },
    sources: {
      list: (query?: ListDataModelSourcesQuery) =>
        resource.listSources((entry as any).dataModelId, query),
      listAll: (query?: ListDataModelSourcesQuery) =>
        resource.listSourcesAll((entry as any).dataModelId, query),
    },
    tagsResource: {
      list: (query?: ListDataModelTagsQuery) =>
        resource.listTags((entry as any).dataModelId, query),
      listAll: (query?: ListDataModelTagsQuery) =>
        resource.listTagsAll((entry as any).dataModelId, query),
    },
    getSpec: (query?: GetDataModelSpecQuery) =>
      resource.getSpec((entry as any).dataModelId, query),
    updateSpec: (body: UpdateDataModelSpecBody) =>
      resource.updateSpec((entry as any).dataModelId, body),
    swapSources: (body: SwapDataModelSourcesBody) =>
      resource.swapSources((entry as any).dataModelId, body),
    sourceSwapV3Alpha: (body: V3alpha_sourceSwapDataModelBody) =>
      resource.sourceSwapV3Alpha((entry as any).dataModelId, body),
    getMaterialization: (materializationId: string) =>
      resource.getMaterialization(
        (entry as any).dataModelId,
        materializationId,
      ),
  }) as HydratedDataModel<T>;
}

export class DataModelsResource {
  constructor(private client: Client<paths>) {}

  /**
   * List data models
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodels
   *
   * This endpoint retrieves a list of all data models available. You can use the response from this endpoint to review existing data models and determine if there are duplicates or gaps.
   *
   * ### Usage notes
   * This endpoint requires no parameters for basic requests, but supports query parameters for pagination and response limit.
   *
   * Users with the Admin account type can optionally retrieve all data models in the organization.
   *
   * #### Pagination
   * This endpoint supports pagination, which lets you retrieve large sets of data in manageable segments. The response includes pagination details as follows:
   * - hasMore: A boolean value indicating whether there are more pages of data available beyond the current page.
   * - total: The total number of entries available across all pages.
   * - nextPage: An identifier or token that you can use in a subsequent request to retrieve the next page of data.
   *
   * #### Example response for pagination
   * {
   *   "hasMore": true,
   *   "total": 104,
   *   "nextPage": "50"
   * }
   * To request additional pages, include the `nextPage` option in your followup request according to the endpoint's parameter requirements. This process can be repeated until the `nextPage` option returns `null`, indicating that no further data is available.
   *
   * ### Usage scenarios
   * - **Data model navigation:** Allows users to easily navigate through their available of data models and access the one they need.
   * - **Track lineage**: Identify dependencies and lineage of data sources and data models.
   *
   * @param query - Query parameters
   * @returns ListDataModelsResponse
   */
  async list(
    query?: ListDataModelsQuery,
  ): Promise<HydratePage<ListDataModelsResponse, DataModelSubResources>> {
    const { data, error, response } = await this.client.GET('/v2/dataModels', {
      params: { query },
    });
    if (error) throw new SigmaApiError(error, response);
    const result = data! as any;
    if (Array.isArray(result)) {
      return result.map((e: any) => _hydrateDataModel(e, this)) as any;
    }
    if (result.entries && Array.isArray(result.entries)) {
      result.entries = result.entries.map((e: any) =>
        _hydrateDataModel(e, this),
      );
    }
    return result;
  }

  /**
   * List data models (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodels
   *
   * This endpoint retrieves a list of all data models available. You can use the response from this endpoint to review existing data models and determine if there are duplicates or gaps.
   *
   * ### Usage notes
   * This endpoint requires no parameters for basic requests, but supports query parameters for pagination and response limit.
   *
   * Users with the Admin account type can optionally retrieve all data models in the organization.
   *
   * #### Pagination
   * This endpoint supports pagination, which lets you retrieve large sets of data in manageable segments. The response includes pagination details as follows:
   * - hasMore: A boolean value indicating whether there are more pages of data available beyond the current page.
   * - total: The total number of entries available across all pages.
   * - nextPage: An identifier or token that you can use in a subsequent request to retrieve the next page of data.
   *
   * #### Example response for pagination
   * {
   *   "hasMore": true,
   *   "total": 104,
   *   "nextPage": "50"
   * }
   * To request additional pages, include the `nextPage` option in your followup request according to the endpoint's parameter requirements. This process can be repeated until the `nextPage` option returns `null`, indicating that no further data is available.
   *
   * ### Usage scenarios
   * - **Data model navigation:** Allows users to easily navigate through their available of data models and access the one they need.
   * - **Track lineage**: Identify dependencies and lineage of data sources and data models.
   *
   * @param query - Query parameters
   * @yields ListDataModelsResponse
   */
  listAll(
    query?: ListDataModelsQuery,
  ): PaginatedAsyncGenerator<
    HydratePage<ListDataModelsResponse, DataModelSubResources>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list({
          ...query,
          page: cursor,
        } as ListDataModelsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Get a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodel
   *
   * Get details of a specific data model by `dataModelId`.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns GetDataModelResponse
   */
  async get(
    dataModelId: string,
    query?: GetDataModelQuery,
  ): Promise<HydratedDataModel<GetDataModelResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return _hydrateDataModel(data! as any, this);
  }

  /**
   * Run a scheduled materialization for a data model element
   *
   * Source docs: https://help.sigmacomputing.com/reference/materializedatamodelelement
   *
   * This endpoint runs a scheduled materialization for an element in a data model. Materialization processes the data of the specified element, allowing the data to be stored or cached for optimized access and performance.
   *
   *   For more details on materialization, see [Materialization](https://help.sigmacomputing.com/docs/materialization).
   *
   *   ### Usage notes
   *   - The materialization schedule for the specified element must be created beforehand.
   *   - Retrieve the **sheetId** by calling the [/v2/dataModels/{dataModelId}/materialization-schedules](listdatamodelmaterializationschedules) endpoint.
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *   - **Performance optimization:** Use this endpoint to improve response times for frequently accessed data model elements.
   *   - **Data refresh:** Allows users to manually (programmatically) refresh the data of specific data model elements to ensure that the latest data is available for analysis and reporting.
   *
   *   ### Best practices
   *   - Prioritize materialization for elements that are heavily used or form critical components of business reports.
   *   - Monitor the performance impacts of materialization and adjust strategies as necessary to optimize resource usage and response times.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param body - The request body.
   * @returns MaterializeDataModelElementResponse
   */
  async materializeElement(
    dataModelId: string,
    body: MaterializeDataModelElementBody,
  ): Promise<DeepReadonly<MaterializeDataModelElementResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/dataModels/{dataModelId}:materialize',
      { params: { path: { dataModelId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List columns for all elements in a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelcolumns
   *
   * This endpoint retrieves all columns in a data model for all elements in the data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *   - **Data exploration:** Enable users to explore the data structure of a data model element, facilitating deeper analysis and understanding.
   *   - **Integration tasks:** Useful for developers integrating Sigma with other tools that need to know the data structure to map data accurately.
   *
   *   ### Best practices
   *   - Use this endpoint when setting up interfaces that require specific knowledge about the data structure.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelColumnsResponse
   */
  async listColumns(
    dataModelId: string,
    query?: ListDataModelColumnsQuery,
  ): Promise<DeepReadonly<ListDataModelColumnsResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/columns',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List columns for all elements in a data model (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelcolumns
   *
   * This endpoint retrieves all columns in a data model for all elements in the data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *   - **Data exploration:** Enable users to explore the data structure of a data model element, facilitating deeper analysis and understanding.
   *   - **Integration tasks:** Useful for developers integrating Sigma with other tools that need to know the data structure to map data accurately.
   *
   *   ### Best practices
   *   - Use this endpoint when setting up interfaces that require specific knowledge about the data structure.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelColumnsResponse
   */
  listColumnsAll(
    dataModelId: string,
    query?: ListDataModelColumnsQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelColumnsResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listColumns(dataModelId, {
          ...query,
          page: cursor,
        } as ListDataModelColumnsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * List elements in a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelelements
   *
   * This endpoint retrieves details of all elements in a data model, which helps you understand and interact with the individual components that make up the data model, such as tables or controls.
   * ### Usage notes
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   * - To retrieve elements from version-tagged data models, pass the `tagName` in the query parameters with the tag name. Retrieve the tag name by calling the [/v2/dataModels/{dataModelId}/tags](listdatamodeltags) endpoint and using the `tags.name` included in the response for a given data model.
   * ### Usage scenarios
   * - **Detailed analysis:** Developers and users can retrieve specific elements for detailed data analysis or further manipulation.
   * - **Interface customization:** Allows for dynamic interface construction where users can choose which elements to display or interact with.
   * ### Best practices
   * - Use pagination to handle large numbers of elements efficiently, reducing load times and improving user experience.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelElementsResponse
   */
  async listElements(
    dataModelId: string,
    query?: ListDataModelElementsQuery,
  ): Promise<DeepReadonly<ListDataModelElementsResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/elements',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List elements in a data model (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelelements
   *
   * This endpoint retrieves details of all elements in a data model, which helps you understand and interact with the individual components that make up the data model, such as tables or controls.
   * ### Usage notes
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   * - To retrieve elements from version-tagged data models, pass the `tagName` in the query parameters with the tag name. Retrieve the tag name by calling the [/v2/dataModels/{dataModelId}/tags](listdatamodeltags) endpoint and using the `tags.name` included in the response for a given data model.
   * ### Usage scenarios
   * - **Detailed analysis:** Developers and users can retrieve specific elements for detailed data analysis or further manipulation.
   * - **Interface customization:** Allows for dynamic interface construction where users can choose which elements to display or interact with.
   * ### Best practices
   * - Use pagination to handle large numbers of elements efficiently, reducing load times and improving user experience.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelElementsResponse
   */
  listElementsAll(
    dataModelId: string,
    query?: ListDataModelElementsQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelElementsResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listElements(dataModelId, {
          ...query,
          page: cursor,
        } as ListDataModelElementsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * List lineage for a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodellineagetree
   *
   * This endpoint lists the elements, data sources, and upstream lineage for those data sources in a data model, providing a detailed view of how data is interconnected within a data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *   - **Data governance:** Allows data managers to track how information is used and propagated through different data model elements.
   *   - **Debugging and optimization:** Helps with identifying and resolving issues in data processing or data model structure.
   *   - **Data source management**: Identify the dependencies of data sources in use in a specific data model.
   *
   *   ### Best practices
   *   - Integrate this information into documentation or data dictionaries to enhance transparency and understanding across teams.
   *   - Use this endpoint to regularly audit data model structures, ensuring that all data connections and transformations are correct and optimized.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelLineageTreeResponse
   */
  async listLineageTree(
    dataModelId: string,
    query?: ListDataModelLineageTreeQuery,
  ): Promise<DeepReadonly<ListDataModelLineageTreeResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/lineage',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List lineage for a data model (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodellineagetree
   *
   * This endpoint lists the elements, data sources, and upstream lineage for those data sources in a data model, providing a detailed view of how data is interconnected within a data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *   - **Data governance:** Allows data managers to track how information is used and propagated through different data model elements.
   *   - **Debugging and optimization:** Helps with identifying and resolving issues in data processing or data model structure.
   *   - **Data source management**: Identify the dependencies of data sources in use in a specific data model.
   *
   *   ### Best practices
   *   - Integrate this information into documentation or data dictionaries to enhance transparency and understanding across teams.
   *   - Use this endpoint to regularly audit data model structures, ensuring that all data connections and transformations are correct and optimized.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelLineageTreeResponse
   */
  listLineageTreeAll(
    dataModelId: string,
    query?: ListDataModelLineageTreeQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelLineageTreeResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listLineageTree(dataModelId, {
          ...query,
          page: cursor,
        } as ListDataModelLineageTreeQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Get a materialization job
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelmaterialization
   *
   * This endpoint retrieves details of a specific materialization job. It provides comprehensive information including status, start and completion times, which helps for monitoring and troubleshooting materialization.
   *
   * ### Usage notes
   * - Retrieve the **materializationId** by calling the [/v2/dataModels/materializations](materializedatamodelelement) endpoint.
   *
   * ### Usage scenarios
   * - **Job Monitoring:** Enables administrators to track the progress and status of materialization jobs, aiding in operational oversight.
   * - **Troubleshooting:** Provides detailed information needed to diagnose and resolve issues in materialization processes.
   *
   * ### Best practices
   * - Monitor materialization jobs regularly to detect and resolve issues promptly.
   * - Use the detailed information provided by this endpoint to optimize materialization settings and performance.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param materializationId - Unique identifier of the materialization job
   * @returns GetDataModelMaterializationResponse
   */
  async getMaterialization(
    dataModelId: string,
    materializationId: string,
  ): Promise<DeepReadonly<GetDataModelMaterializationResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/materializations/{materializationId}',
      { params: { path: { dataModelId, materializationId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List materialization schedules for a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelmaterializationschedules
   *
   *
   *   This endpoint retrieves a list of all materialization schedules associated with a data model. Each schedule details when and how often specific data model elements are automatically materialized to optimize performance and data freshness.
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *   - To retrieve additional pages of results, use the response of the `nextPageToken` parameter as the `pageToken` query parameter in the following request.
   *
   *   ### Usage scenarios
   *   - **Monitoring and management:** Administrators can monitor and manage the schedules for materialization to ensure optimal system performance.
   *   - **Audit and review:** Periodic reviews of materialization schedules can help in assessing the efficiency of data processes and making necessary adjustments.
   *
   *   ### Best practices
   *   - Regularly review materialization schedules to align them with current data usage patterns and business needs.
   *   - Use pagination to manage large sets of data and improve response times of API calls.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelMaterializationSchedulesResponse
   */
  async listMaterializationSchedules(
    dataModelId: string,
    query?: ListDataModelMaterializationSchedulesQuery,
  ): Promise<DeepReadonly<ListDataModelMaterializationSchedulesResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/materializationSchedules',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List materialization schedules for a data model (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelmaterializationschedules
   *
   *
   *   This endpoint retrieves a list of all materialization schedules associated with a data model. Each schedule details when and how often specific data model elements are automatically materialized to optimize performance and data freshness.
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *   - To retrieve additional pages of results, use the response of the `nextPageToken` parameter as the `pageToken` query parameter in the following request.
   *
   *   ### Usage scenarios
   *   - **Monitoring and management:** Administrators can monitor and manage the schedules for materialization to ensure optimal system performance.
   *   - **Audit and review:** Periodic reviews of materialization schedules can help in assessing the efficiency of data processes and making necessary adjustments.
   *
   *   ### Best practices
   *   - Regularly review materialization schedules to align them with current data usage patterns and business needs.
   *   - Use pagination to manage large sets of data and improve response times of API calls.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelMaterializationSchedulesResponse
   */
  listMaterializationSchedulesAll(
    dataModelId: string,
    query?: ListDataModelMaterializationSchedulesQuery,
  ): PaginatedAsyncGenerator<
    DeepReadonly<ListDataModelMaterializationSchedulesResponse>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listMaterializationSchedules(dataModelId, {
          ...query,
          pageToken: cursor,
        } as ListDataModelMaterializationSchedulesQuery);
        yield page;
        cursor = (page as { nextPageToken?: string | null })[
          'nextPageToken'
        ] as string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * List data model sources
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelsources
   *
   * List data sources of a data model given the dataModelId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelSourcesResponse
   */
  async listSources(
    dataModelId: string,
    query?: ListDataModelSourcesQuery,
  ): Promise<DeepReadonly<ListDataModelSourcesResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/sources',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List data model sources (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodelsources
   *
   * List data sources of a data model given the dataModelId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelSourcesResponse
   */
  listSourcesAll(
    dataModelId: string,
    query?: ListDataModelSourcesQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelSourcesResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listSources(dataModelId, {
          ...query,
          pageToken: cursor,
        } as ListDataModelSourcesQuery);
        yield page;
        cursor = (page as { nextPageToken?: string | null })[
          'nextPageToken'
        ] as string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Get the code representation of a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/getdatamodelspec
   *
   * This endpoint returns a code representation of an existing data model, including metadata and contents. The response can be formatted as either JSON or YAML, and contains a data model's pages, elements, columns, sources, and more. The representation can be used to store, copy, or modify the data model programmatically.
   *
   * For more information on managing data models via the Sigma API, see [Manage data models as code](https://help.sigmacomputing.com/docs/manage-data-models-as-code). For more information on using this endpoint, including an end-to-end example, see [Get the code representation of a data model](https://help.sigmacomputing.com/docs/get-the-code-representation-of-a-data-model).
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user with **Can view** access to the data model.
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint. Invalid or nonexistent IDs return an error.
   * - The response includes the data sources, metadata, and relationships necessary to recreate the data model. The data itself is not included in the response.
   * - To create a new data model from code, use the [/v2/dataModels/spec](createdatamodelspec) endpoint.
   * - To update an existing data model from code, use the [/v2/dataModels/{dataModelId}/spec](updatedatamodelspec) endpoint.
   * - The default format of the representation is JSON. To receive YAML, add the query parameter `?format=yaml` or the header `Accept: application/yaml`.
   *
   * ### Usage scenarios
   * - **Backup and restore**: Developers can use this endpoint to programmatically back up data models and restore them if needed.
   * - **Version control**: Developers can use this endpoint to programmatically manage, update, and version control data models between several organizations or tenants.
   * - **Administration**: Administrators can use this endpoint to programmatically analyze and govern data models across an organization.
   * - **Migration**: Administrators can use this endpoint to programmatically migrate data models between organizations or tenants.
   * - **Agentic workflows**: Agents can use these endpoints to directly manage data model contents in code.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns GetDataModelSpecResponse
   */
  async getSpec(
    dataModelId: string,
    query?: GetDataModelSpecQuery,
  ): Promise<DeepReadonly<GetDataModelSpecResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/spec',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Update a data model from a code representation
   *
   * Source docs: https://help.sigmacomputing.com/reference/updatedatamodelspec
   *
   * This endpoint replaces a data model with the contents defined in a code representation. You can programmatically update the data model's structure, metadata, and contents. This creates a new version of the data model, which is accessible from the Sigma UI.
   *
   * For more information on managing data models via the Sigma API, see [Manage data models as code](https://help.sigmacomputing.com/docs/manage-data-models-as-code). For more information on using this endpoint, including an end-to-end example, see [Update a data model from a code representation](https://help.sigmacomputing.com/docs/update-a-data-model-from-a-code-representation).
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned an account type with **Create, edit, and publish data models** permission.
   * - To perform this operation, you must use API credentials owned by a user with **Can edit** access to the data model.
   * - Retrieve a representation of an existing data model by calling the [/v2/dataModels/{dataModelId}/spec](getdatamodelspec) endpoint.
   * - To create a new data model from code, use the [/v2/dataModels/spec](createdatamodelspec) endpoint.
   * - The endpoint only supports updates to the `pages` array and its contents. Additional fields are not required and are ignored in the request body.
   * - Partial updates are not supported. You must provide the entire representation of the data model in the request body.
   * - The default format of the representation is JSON. To use YAML, add the header `Content-Type: application/yaml`.
   *
   * ### Usage scenarios
   * - **Automation**: If you want to update several data models across one or more organizations, or in response to external events, you can use this endpoint to programmatically update the data models.
   * - **Integration**: Using this endpoint, you can update data models based on external data sources or integrate Sigma with other tools and platforms.
   * - **Version control**: Developers can use this endpoint to programmatically manage, update, and version control data models between several organizations or tenants.
   * - **Agentic workflows**: Agents can use these endpoints to directly manage data model contents in code.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param body - The request body.
   * @returns UpdateDataModelSpecResponse
   */
  async updateSpec(
    dataModelId: string,
    body: UpdateDataModelSpecBody,
  ): Promise<DeepReadonly<UpdateDataModelSpecResponse>> {
    const { data, error, response } = await this.client.PUT(
      '/v2/dataModels/{dataModelId}/spec',
      { params: { path: { dataModelId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Swap data model sources
   *
   * Source docs: https://help.sigmacomputing.com/reference/swapdatamodelsources
   *
   * Swap the data sources used by a data model. You can swap from one connection to another, and/or from one dataset or table to another.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *   - Retrieve the **connectionId** by calling the [/v2/connections](listconnections) endpoint.
   *   - To map the sources of a specific element, you can use the table or dataset ID:
   *
   *     - List sources of data model by calling the [/v2/dataModels/{dataModelId}/sources](listdatamodelsources) endpoint.
   *     - Retrieve table and dataset IDs by calling the [/v2/files](fileslist) endpoint. Use the `typeFilters` option to filter for table and dataset files. Use `id` field in the response as the `fromId` or `toId`, as relevant.
   *
   *   - To swap all tables in a connection, set the `swapTables` option to `true`.
   *
   *     - Use the `paths` option to specify the connection paths to swap. The connection path is a list of strings that represents the path to the table in the cloud data warehouse. For example, `["database", "schema", "table"]`.
   *     - If the table schema is the same in both connections, leave the `paths` option empty to swap all tables in the connection. You can also pass a partial path to swap between databases, catalogs, or schemas.
   *     - If `sourceMapping` is provided, those mappings take precedence over the connection swaps.
   *     - If `swapTables` is not set to `true`, only Custom SQL elements have their connections changed.
   *
   *   ### Usage scenarios
   *   - **Data model development**: Use a test data source while developing a data model, then swap the source to a production database when ready to share the data model for production use.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param body - The request body.
   * @returns SwapDataModelSourcesResponse
   */
  async swapSources(
    dataModelId: string,
    body: SwapDataModelSourcesBody,
  ): Promise<DeepReadonly<SwapDataModelSourcesResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/dataModels/{dataModelId}/swapSources',
      { params: { path: { dataModelId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List tags for a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodeltags
   *
   * Retrieve the version tag information for a specific data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *
   *   - Provide on-demand performant access to version tag details for a data model. For example, make a GET request to the [/v2/dataModels](listdatamodels) endpoint with the `excludeTags` parameter set to `True` to improve performance, then use this endpoint to return tag information for specific data models.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @returns ListDataModelTagsResponse
   */
  async listTags(
    dataModelId: string,
    query?: ListDataModelTagsQuery,
  ): Promise<DeepReadonly<ListDataModelTagsResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/dataModels/{dataModelId}/tags',
      { params: { path: { dataModelId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List tags for a data model (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listdatamodeltags
   *
   * Retrieve the version tag information for a specific data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *
   *   ### Usage scenarios
   *
   *   - Provide on-demand performant access to version tag details for a data model. For example, make a GET request to the [/v2/dataModels](listdatamodels) endpoint with the `excludeTags` parameter set to `True` to improve performance, then use this endpoint to return tag information for specific data models.
   *
   * @param dataModelId - Unique identifier of the data model.
   * @param query - Query parameters
   * @yields ListDataModelTagsResponse
   */
  listTagsAll(
    dataModelId: string,
    query?: ListDataModelTagsQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListDataModelTagsResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listTags(dataModelId, {
          ...query,
          page: cursor,
        } as ListDataModelTagsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create a data model from a code representation
   *
   * Source docs: https://help.sigmacomputing.com/reference/createdatamodelspec
   *
   * This endpoint creates a new data model in Sigma from a code representation. Use it to programmatically define and create data models that are then accessible from the Sigma UI.
   *
   * For more information on managing data models via the Sigma API, see [Manage data models as code](https://help.sigmacomputing.com/docs/manage-data-models-as-code). For more information on using this endpoint, including an end-to-end example, see [Create a data model from a code representation](https://help.sigmacomputing.com/docs/create-a-data-model-from-a-code-representation).
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned an account type with **Create, edit, and publish data models** permission.
   * - Retrieve a representation of an existing data model by calling the [/v2/dataModels/{dataModelId}/spec](getdatamodelspec) endpoint.
   * - To get a list of folders, call the [/v2/files](fileslist) endpoint and review the `id` field in the response for files with a `type` of `folder`.
   * - The default format of the representation is JSON. To use YAML, add the header `Content-Type: application/yaml`.
   *
   * ### Usage scenarios
   * - **Automation**: If you want to create several data models across one or more organizations, you can use this endpoint to programmatically create the data models.
   * - **Integration**: Using this endpoint, you can create data models based on external data sources or integrate Sigma with other tools and platforms.
   * - **Migration**: You can use this endpoint to migrate data models from one organization to another.
   * - **Version control**: Developers can use this endpoint to programmatically manage, update, and version control data models between several organizations or tenants.
   * - **Agentic workflows**: Agents can use these endpoints to directly manage data model contents in code.
   *
   * @param body - The request body.
   * @returns CreateDataModelSpecResponse
   */
  async createSpec(
    body: CreateDataModelSpecBody,
  ): Promise<DeepReadonly<CreateDataModelSpecResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/dataModels/spec',
      { body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Tag a data model
   *
   * Source docs: https://help.sigmacomputing.com/reference/tagdatamodel
   *
   * Add a version tag to a data model and optionally set up a connection to swap to for a specific version of the data model.
   *
   *   ### Usage notes
   *   - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   *   - Retrieve the **tag** by calling the [/v2/tags](listversiontag) endpoint and using the `name` in the response.
   *   - Retrieve the **connectionId** to use as the **fromId** or **toId** by calling the [/v2/connections](listconnections) endpoint.
   *   - If your data model includes a source that is not mapped to a new source, that source is not swapped.
   *   - When swapping data models used as the source for the data model:
   *     - You can only swap a data model source to another version of the same data model source. You cannot swap a data model source to a table in your data warehouse or a dataset.
   *     - When you swap sources from one data model version to a new one, specify the version tag of the data model that you swap to with `toVersionTagId`:
   *       - To swap to the latest published version of the data model, specify `toVersionTagId` as `null`.
   *       - To swap to a specific tagged version of the data model, specify the `toVersionTagId` of the data model.
   *     - If the data model already uses a specific tagged version of a data model as a source, use `fromVersionTagId` to indicate which tagged version to swap from.
   *       - To swap from the latest published version of the data model, specify `fromVersionTagId` as `null`.
   *       - To swap from a specific tagged version of the data model, specify the `fromVersionTagId` of the data model.
   *   - To retrieve the `fromVersionTagId` for a data model used as the data model source, call the [/v2/dataModels/{dataModelId}/sources](listdatamodelsources) endpoint and use the `versionTagId` in the response.
   *   - To retrieve the `versionTagId` for a data model, call the [/v2/dataModels](listdatamodels) endpoint and use the `versionTagId` in the response.
   *
   *   ### Usage scenarios
   *   - **Lifecycle management**: Identify production and development resources.
   *
   * @param body - The request body.
   * @returns TagDataModelResponse
   */
  async tag(
    body: TagDataModelBody,
  ): Promise<DeepReadonly<TagDataModelResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/dataModels/tag',
      { body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Swap data model sources (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/v3alpha_sourceswapdatamodel
   *
   * Swap each data source used by a data model. You can swap from one table, dataset, data model element, or custom SQL element to another for each data source.
   *
   * **Beta**: This documentation describes a private beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - Retrieve the **dataModelId** by calling the [/v2/dataModels](listdatamodels) endpoint.
   * - Retrieve data model sources by calling the [/v2/dataModels/{dataModelId}/sources](listdatamodelsources) endpoint.
   * - Retrieve the **datasetId** by calling the [/v2/datasets](listdatasets) endpoint.
   * - Retrieve the **tableId** by calling the [/v2/connection](lookupconnection) endpoint.
   * - Retrieve the **connectionId** for a new custom SQL element by calling the [/v2/connections](listconnections) endpoint.
   *
   * - Use the `sourceMapping` options in the request body to swap specific sources. For each source in the source mapping, provide the current source in use in the **from** object, and provide the source that you want to use instead in the **to** object.
   * - If your source is a data model with one or more tagged versions, you can also provide a `versionTagId` to swap to or from a tagged version of the data model. The data model version must already be tagged.
   * - When swapping **from** a custom SQL element, identify it by its element ID using the `customSqlId` field. When swapping **to** a custom SQL element, supply the `connectionId` of the connection to run against and the SQL query in the `definition` field.
   * - If your source is a data model or a dataset, you can use the `metricMapping` option to map metrics that rely on column names from the old source to the new one so that they continue to work.
   * - If the source that you swap from uses column names, links, or relationships different from the source that you are swapping to, use the `columnMapping` option to map each column in the original source to a corresponding column in the new source.
   * - When `columnMapping` is omitted, columns are automatically matched by display name, ignoring differences in case and punctuation. For example, a custom SQL output column named `DOCK_COUNT` automatically maps to a column with display name `Dock Count` in the target source. Provide `columnMapping` explicitly to override the automatic match for a column.
   *
   * ### Usage scenarios
   * - **Data model development**: Use a test data source during development, then swap to a production source when ready.
   * - **Dataset migration**: Update references to legacy dataset sources with data model sources by mapping dataset IDs to their new data model equivalents.
   *
   * @param dataModelId - The identifier of the data model.
   * @param body - The request body.
   * @returns V3alpha_sourceSwapDataModelResponse
   */
  async sourceSwapV3Alpha(
    dataModelId: string,
    body: V3alpha_sourceSwapDataModelBody,
  ): Promise<DeepReadonly<V3alpha_sourceSwapDataModelResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v3alpha/dataModels/{dataModelId}:swapSources',
      { params: { path: { dataModelId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }
}
