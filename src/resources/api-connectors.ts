// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths, operations, components } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import { PaginatedAsyncGenerator, DeepReadonly } from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link ApiConnectorsResource["list"] | list}. */
export interface ListApiConnectorsQuery {
  /**
   * Use to specify the next set of results with the string returned in the `nextPageToken` field of the previous response.
   */
  pageToken?: string;
  /** Number of results to return per page, with a maximum of 1000. Defaults to 50. */
  pageSize?: number;
  /**
   * Sort order. Supported fields: `name`, `description`. Append ` desc` for descending (e.g. `name desc`). Defaults to `name` ascending.
   */
  orderBy?: string;
  /** Filter results to connectors whose name contains this substring. */
  name?: string;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link ApiConnectorsResource["create"] | create}. */
export type CreateApiConnectorBody = NonNullable<
  operations['createApiConnector']['requestBody']
>['content']['application/json'];

/** Request body for {@link ApiConnectorsResource["update"] | update}. */
export type UpdateApiConnectorBody = NonNullable<
  operations['updateApiConnector']['requestBody']
>['content']['application/json'];

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

/** Response from {@link ApiConnectorsResource["list"] | list}. */
export interface ListApiConnectorsResponse {
  /** Array of results returned by the endpoint */
  entries: {
    apiConnectorId: string;
    name: string;
    description: string;
  }[];
  /**
   * A string that can be passed to the `pageToken` parameter in the next request to fetch the next page of results. Not present in the last page of results.
   * **Must be treated as an opaque string.**
   */
  nextPageToken?: string;
}

/** Response from {@link ApiConnectorsResource["create"] | create}. */
export interface CreateApiConnectorResponse {
  /** Unique identifier of the API connector. */
  apiConnectorId: string;
  /** Display name of the connector. */
  name: string;
  params: {
    method: 'GET' | 'POST' | 'DELETE' | 'PATCH' | 'PUT' | 'OPTIONS' | 'HEAD';
    url: string;
    headers: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    pathParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    queryParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    body: string;
    bodyParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
  };
  config: {
    timeout: {
      requestSec: number;
    };
    retry: {
      maxRetries: number;
      retryableStatusCodes: number[];
    };
    redirects: {
      maxRedirects: number;
    };
    rateLimit: {
      maxRequestsPerWindow: number;
    };
  };
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  /** Human-readable description. */
  description?: string;
  /**
   * Identifier of the API credential (`apiCredentialId`) used to authenticate requests. `null` if unauthenticated.
   */
  authId?: string | null;
}

/** Response from {@link ApiConnectorsResource["get"] | get}. */
export interface GetApiConnectorResponse {
  /** Unique identifier of the API connector. */
  apiConnectorId: string;
  /** Display name of the connector. */
  name: string;
  params: {
    method: 'GET' | 'POST' | 'DELETE' | 'PATCH' | 'PUT' | 'OPTIONS' | 'HEAD';
    url: string;
    headers: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    pathParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    queryParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    body: string;
    bodyParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
  };
  config: {
    timeout: {
      requestSec: number;
    };
    retry: {
      maxRetries: number;
      retryableStatusCodes: number[];
    };
    redirects: {
      maxRedirects: number;
    };
    rateLimit: {
      maxRequestsPerWindow: number;
    };
  };
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  /** Human-readable description. */
  description?: string;
  /**
   * Identifier of the API credential (`apiCredentialId`) used to authenticate requests. `null` if unauthenticated.
   */
  authId?: string | null;
}

/** Response from {@link ApiConnectorsResource["update"] | update}. */
export interface UpdateApiConnectorResponse {
  /** Unique identifier of the API connector. */
  apiConnectorId: string;
  /** Display name of the connector. */
  name: string;
  params: {
    method: 'GET' | 'POST' | 'DELETE' | 'PATCH' | 'PUT' | 'OPTIONS' | 'HEAD';
    url: string;
    headers: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    pathParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    queryParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
    body: string;
    bodyParams: {
      key: string;
      mode: 'static' | 'dynamic';
      value?: string | null;
      type?:
        'array' | 'boolean' | 'number' | 'object' | 'raw' | 'string' | null;
      shape?: components['schemas']['ParamShapeNode'] | null;
    }[];
  };
  config: {
    timeout: {
      requestSec: number;
    };
    retry: {
      maxRetries: number;
      retryableStatusCodes: number[];
    };
    redirects: {
      maxRedirects: number;
    };
    rateLimit: {
      maxRequestsPerWindow: number;
    };
  };
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  /** Human-readable description. */
  description?: string;
  /**
   * Identifier of the API credential (`apiCredentialId`) used to authenticate requests. `null` if unauthenticated.
   */
  authId?: string | null;
}

/** Response from {@link ApiConnectorsResource["delete"] | delete}. */
export type DeleteApiConnectorResponse = Record<string, never>;

// ---------------------------------------------------------------------------
// ApiConnectorsResource
// ---------------------------------------------------------------------------

export class ApiConnectorsResource {
  constructor(private client: Client<paths>) {}

  /**
   * List API connectors
   *
   * Source docs: https://help.sigmacomputing.com/reference/listapiconnectors
   *
   * This endpoint returns a paginated list of API connectors. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - Only returns API connectors that the user making this request has at least **Can view** access to.
   * - Use the `name` query parameter to filter by connector name.
   * - Use the `orderBy` query parameter to set sort order.
   *
   * ### Usage scenarios
   * - **Connector discovery:** Retrieve a list of available API connectors available to users configuring **Call API** actions.
   * - **Connector management:** View and audit the organization's API connectors.
   *
   * @param query - Query parameters
   * @returns ListApiConnectorsResponse
   */
  async list(
    query?: ListApiConnectorsQuery,
  ): Promise<DeepReadonly<ListApiConnectorsResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/api-connectors',
      { params: { query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List API connectors (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listapiconnectors
   *
   * This endpoint returns a paginated list of API connectors. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - Only returns API connectors that the user making this request has at least **Can view** access to.
   * - Use the `name` query parameter to filter by connector name.
   * - Use the `orderBy` query parameter to set sort order.
   *
   * ### Usage scenarios
   * - **Connector discovery:** Retrieve a list of available API connectors available to users configuring **Call API** actions.
   * - **Connector management:** View and audit the organization's API connectors.
   *
   * @param query - Query parameters
   * @yields ListApiConnectorsResponse
   */
  listAll(
    query?: ListApiConnectorsQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListApiConnectorsResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list({
          ...query,
          pageToken: cursor,
        } as ListApiConnectorsQuery);
        yield page;
        cursor = (page as { nextPageToken?: string | null })[
          'nextPageToken'
        ] as string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create an API connector
   *
   * Source docs: https://help.sigmacomputing.com/reference/createapiconnector
   *
   * This endpoint creates a new API connector that defines how to call an external HTTP endpoint from within Sigma. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - The user making this request must be assigned an account type with the **Manage API connectors** permission enabled.
   * - If a credential is provided using the `authId` parameter, the user making this request must have at least **Can view** permission on the credential and the request URL must match the credential's allowlist.
   * - Retrieve the **apiCredentialId** (used as `authId`) by calling the [/v2/api-credentials](listapicredentials) endpoint.
   *
   * ### Usage scenarios
   * - **Programmatic connector setup:** Automate creation of API connectors as part of an environment provisioning workflow.
   * - **Integration onboarding:** Create connectors for each external service your workbooks need to interact with.
   *
   * @param body - The request body.
   * @returns CreateApiConnectorResponse
   */
  async create(
    body: CreateApiConnectorBody,
  ): Promise<DeepReadonly<CreateApiConnectorResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/api-connectors',
      { body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Get an API connector
   *
   * Source docs: https://help.sigmacomputing.com/reference/getapiconnector
   *
   * This endpoint returns full details for a single API connector, including its request parameters and configuration. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - Only returns API connectors that the user making this request has at least **Can view** access to.
   * - Retrieve the **apiConnectorId** by calling the [/v2/api-connectors](listapiconnectors) endpoint.
   *
   * ### Usage scenarios
   * - **Connector inspection:** Retrieve the full configuration of a connector for display or validation before use.
   * - **Connector duplication:** Read an existing connector's configuration to use as the basis for a new one.
   *
   * @param apiConnectorId
   * @returns GetApiConnectorResponse
   */
  async get(
    apiConnectorId: string,
  ): Promise<DeepReadonly<GetApiConnectorResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/api-connectors/{apiConnectorId}',
      { params: { path: { apiConnectorId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Update an API connector
   *
   * Source docs: https://help.sigmacomputing.com/reference/updateapiconnector
   *
   * This endpoint updates one or more fields on an existing API connector. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - Retrieve the **apiConnectorId** by calling the [/v2/api-connectors](listapiconnectors) endpoint.
   * - The user making this request must be assigned an account type with the **Manage API connectors** permission enabled and must have **Can edit** access to the connector.
   * - Omitted fields are left unchanged. Pass `null` for `authId` to remove the credential association.
   *
   * ### Usage scenarios
   * - **Endpoint migration:** Update the connector URL or parameters when an external API changes its interface.
   * - **Credential rotation:** Rebind the connector to a new credential after rotating secrets.
   *
   * @param apiConnectorId
   * @param body - The request body.
   * @returns UpdateApiConnectorResponse
   */
  async update(
    apiConnectorId: string,
    body: UpdateApiConnectorBody,
  ): Promise<DeepReadonly<UpdateApiConnectorResponse>> {
    const { data, error, response } = await this.client.PATCH(
      '/v2/api-connectors/{apiConnectorId}',
      { params: { path: { apiConnectorId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Delete an API connector
   *
   * Source docs: https://help.sigmacomputing.com/reference/deleteapiconnector
   *
   * This endpoint archives an API connector, preventing it from being used in new workbook actions. For more information on API connectors, see [Configure API credentials and connectors in Sigma](https://help.sigmacomputing.com/docs/configure-api-credentials-and-connectors-in-sigma).
   *
   * ### Usage notes
   * - Retrieve the **apiConnectorId** by calling the [/v2/api-connectors](listapiconnectors) endpoint.
   * - The user making this request must be assigned an account type with the **Manage API connectors** permission enabled and must have **Can edit** access to the connector.
   *
   * ### Usage scenarios
   * - **Cleanup:** Remove connectors that are no longer in use to keep the organization's connector list tidy.
   * - **Decommissioning:** Archive connectors associated with deprecated or retired external services.
   *
   * @param apiConnectorId
   * @returns DeleteApiConnectorResponse
   */
  async delete(
    apiConnectorId: string,
  ): Promise<DeepReadonly<DeleteApiConnectorResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/api-connectors/{apiConnectorId}',
      { params: { path: { apiConnectorId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }
}
