// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import {
  PaginatedAsyncGenerator,
  DeepReadonly,
  HydratePage,
} from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link TagsResource["list"] | list}. */
export interface ListVersionTagQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  /** Search query */
  search?: string;
}

/** Query parameters for {@link TagsResource["listWorkbooks"] | listWorkbooks}. */
export interface ListWorkbooksForTagQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link TagsResource["create"] | create}. */
export interface CreateVersionTagBody {
  /** Name of the tag. Must be unique */
  name: string;
  /** Color to associate with the tag in Sigma */
  color: 'cyan' | 'grass' | 'violet' | 'plum' | 'amber' | 'bronze';
  /** Description for the tag */
  description?: string;
}

/** Request body for {@link TagsResource["update"] | update}. */
export interface UpdateVersionTagBody {
  /** Description of the version tag. */
  description?: string;
}

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

/** Response from {@link TagsResource["list"] | list}. */
export interface ListVersionTagResponse {
  /** Array of results returned by the endpoint */
  entries: {
    versionTagId: string;
    name: string;
    ownerId: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
    isArchived?: boolean;
    description?: string;
    color?: 'cyan' | 'grass' | 'violet' | 'plum' | 'amber' | 'bronze';
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

/** Response from {@link TagsResource["create"] | create}. */
export interface CreateVersionTagResponse {
  name: string;
  versionTagId: string;
}

/** Response from {@link TagsResource["update"] | update}. */
export interface UpdateVersionTagResponse {
  versionTagId: string;
  name: string;
  ownerId: string;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  isArchived?: boolean;
  description?: string;
  color?: 'cyan' | 'grass' | 'violet' | 'plum' | 'amber' | 'bronze';
}

/** Response from {@link TagsResource["delete"] | delete}. */
export type DeleteVersionTagResponse = Record<string, never>;

/** Response from {@link TagsResource["listWorkbooks"] | listWorkbooks}. */
export interface ListWorkbooksForTagResponse {
  /** Array of results returned by the endpoint */
  entries: {
    workbookId: string;
    workbookUrlId: string;
    name: string;
    url: string;
    path: string;
    latestVersion: number;
    ownerId: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
    isArchived?: boolean;
    tags?: {
      versionTagId: string;
      name: string;
      sourceWorkbookVersion: number;
      taggedWorkbookId: string;
      workbookTaggedAt: string;
    }[];
    description?: string;
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

// ---------------------------------------------------------------------------
// Sub-resource accessors (hydration)
// ---------------------------------------------------------------------------

/** Sub-resource accessors available on hydrated tag entries. */
export interface TagSubResources {
  workbooks: {
    /**
     * List workbooks for a tag
     *
     * Source docs: https://help.sigmacomputing.com/reference/listworkbooksfortag
     *
     * Get a list of workbooks for a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
     *
     * @param query - Query parameters
     * @returns ListWorkbooksForTagResponse
     */
    list(
      query?: ListWorkbooksForTagQuery,
    ): Promise<DeepReadonly<ListWorkbooksForTagResponse>>;
    /**
     * List workbooks for a tag (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listworkbooksfortag
     *
     * Get a list of workbooks for a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
     *
     * @param query - Query parameters
     * @yields ListWorkbooksForTagResponse
     */
    listAll(
      query?: ListWorkbooksForTagQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListWorkbooksForTagResponse>>;
  };
}

/** A tag entry with sub-resource accessor methods. Response data properties are deeply readonly. */
export type HydratedTag<T> = DeepReadonly<T> & TagSubResources;

// ---------------------------------------------------------------------------
// TagsResource
// ---------------------------------------------------------------------------

/** @internal Attach sub-resource accessors to a tag entry. */
function _hydrateTag<T extends object>(
  entry: T,
  resource: TagsResource,
): HydratedTag<T> {
  return Object.assign(entry, {
    workbooks: {
      list: (query?: ListWorkbooksForTagQuery) =>
        resource.listWorkbooks((entry as any).tagId, query),
      listAll: (query?: ListWorkbooksForTagQuery) =>
        resource.listWorkbooksAll((entry as any).tagId, query),
    },
  }) as HydratedTag<T>;
}

export class TagsResource {
  constructor(private client: Client<paths>) {}

  /**
   * Get tags
   *
   * Source docs: https://help.sigmacomputing.com/reference/listversiontag
   *
   * Get a list of version tags.
   *
   * @param query - Query parameters
   * @returns ListVersionTagResponse
   */
  async list(
    query?: ListVersionTagQuery,
  ): Promise<HydratePage<ListVersionTagResponse, TagSubResources>> {
    const { data, error, response } = await this.client.GET('/v2/tags', {
      params: { query },
    });
    if (error) throw new SigmaApiError(error, response);
    const result = data! as any;
    if (Array.isArray(result)) {
      return result.map((e: any) => _hydrateTag(e, this)) as any;
    }
    if (result.entries && Array.isArray(result.entries)) {
      result.entries = result.entries.map((e: any) => _hydrateTag(e, this));
    }
    return result;
  }

  /**
   * Get tags (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listversiontag
   *
   * Get a list of version tags.
   *
   * @param query - Query parameters
   * @yields ListVersionTagResponse
   */
  listAll(
    query?: ListVersionTagQuery,
  ): PaginatedAsyncGenerator<
    HydratePage<ListVersionTagResponse, TagSubResources>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list({
          ...query,
          page: cursor,
        } as ListVersionTagQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create a tag
   *
   * Source docs: https://help.sigmacomputing.com/reference/createversiontag
   *
   * Create a version tag to use, for example, for workbooks.
   *
   * @param body - The request body.
   * @returns CreateVersionTagResponse
   */
  async create(
    body: CreateVersionTagBody,
  ): Promise<HydratedTag<CreateVersionTagResponse>> {
    const { data, error, response } = await this.client.POST('/v2/tags', {
      body,
    });
    if (error) throw new SigmaApiError(error, response);
    return _hydrateTag(data! as any, this);
  }

  /**
   * Update a tag
   *
   * Source docs: https://help.sigmacomputing.com/reference/updateversiontag
   *
   * Update the description of a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
   *
   * @param tagId
   * @param body - The request body.
   * @returns UpdateVersionTagResponse
   */
  async update(
    tagId: string,
    body: UpdateVersionTagBody,
  ): Promise<HydratedTag<UpdateVersionTagResponse>> {
    const { data, error, response } = await this.client.PATCH(
      '/v2/tags/{tagId}',
      { params: { path: { tagId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return _hydrateTag(data! as any, this);
  }

  /**
   * Delete a tag
   *
   * Source docs: https://help.sigmacomputing.com/reference/deleteversiontag
   *
   * Delete a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
   *
   * @param tagId
   * @returns DeleteVersionTagResponse
   */
  async delete(tagId: string): Promise<DeepReadonly<DeleteVersionTagResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/tags/{tagId}',
      { params: { path: { tagId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List workbooks for a tag
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkbooksfortag
   *
   * Get a list of workbooks for a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
   *
   * @param tagId
   * @param query - Query parameters
   * @returns ListWorkbooksForTagResponse
   */
  async listWorkbooks(
    tagId: string,
    query?: ListWorkbooksForTagQuery,
  ): Promise<DeepReadonly<ListWorkbooksForTagResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/tags/{tagId}/workbooks',
      { params: { path: { tagId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List workbooks for a tag (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkbooksfortag
   *
   * Get a list of workbooks for a specific version tag. Retrieve the **tagId** by calling the [/v2/tags](listversiontag) endpoint and using the `versionTagId` in the response.
   *
   * @param tagId
   * @param query - Query parameters
   * @yields ListWorkbooksForTagResponse
   */
  listWorkbooksAll(
    tagId: string,
    query?: ListWorkbooksForTagQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListWorkbooksForTagResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listWorkbooks(tagId, {
          ...query,
          page: cursor,
        } as ListWorkbooksForTagQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }
}
