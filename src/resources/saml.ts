// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import { PaginatedAsyncGenerator, DeepReadonly } from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link SamlResource["listServiceProviders"] | listServiceProviders}. */
export interface ListSamlServiceProvidersQuery {
  /**
   * Use to specify the next set of results with the string returned in the `nextPageToken` field of the previous response.
   */
  pageToken?: string;
  /** Number of results to return per page, with a maximum of 500. Defaults to 50. */
  pageSize?: number;
}

/** Query parameters for {@link SamlResource["listServiceProviderCertificates"] | listServiceProviderCertificates}. */
export interface ListSamlServiceProviderCertificatesQuery {
  /**
   * Use to specify the next set of results with the string returned in the `nextPageToken` field of the previous response.
   */
  pageToken?: string;
  /** Number of results to return per page, with a maximum of 500. Defaults to 50. */
  pageSize?: number;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link SamlResource["createServiceProviderCertificate"] | createServiceProviderCertificate}. */
export interface CreateSamlServiceProviderCertificateBody {
  /**
   * Purpose of the certificate. Use `signing` for signing SAML requests and `encryption` for encrypting SAML responses.
   */
  purpose: 'signing' | 'encryption';
  /** Number of days the certificate is valid */
  validForDays: number;
}

/** Request body for {@link SamlResource["activateServiceProviderCertificate"] | activateServiceProviderCertificate}. */
export type ActivateSamlServiceProviderCertificateBody = Record<string, never>;

/** Request body for {@link SamlResource["deactivateServiceProviderCertificate"] | deactivateServiceProviderCertificate}. */
export type DeactivateSamlServiceProviderCertificateBody = Record<
  string,
  never
>;

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

/** Response from {@link SamlResource["listServiceProviders"] | listServiceProviders}. */
export interface ListSamlServiceProvidersResponse {
  /** Array of results returned by the endpoint */
  entries: {
    samlServiceProviderId: string;
  }[];
  /**
   * A string that can be passed to the `pageToken` parameter in the next request to fetch the next page of results. Not present in the last page of results.
   * **Must be treated as an opaque string.**
   */
  nextPageToken?: string;
}

/** Response from {@link SamlResource["listServiceProviderCertificates"] | listServiceProviderCertificates}. */
export interface ListSamlServiceProviderCertificatesResponse {
  /** Array of results returned by the endpoint */
  entries: {
    samlServiceProviderCertificateId: string;
    samlServiceProviderId: string;
    purpose: 'signing' | 'encryption';
    publicKeyCertificate: string;
    isActive: boolean;
    notBefore: string;
    notAfter: string;
    createdAt: string;
    createdBy: string;
    updatedAt: string;
    updatedBy: string;
  }[];
  /**
   * A string that can be passed to the `pageToken` parameter in the next request to fetch the next page of results. Not present in the last page of results.
   * **Must be treated as an opaque string.**
   */
  nextPageToken?: string;
}

/** Response from {@link SamlResource["createServiceProviderCertificate"] | createServiceProviderCertificate}. */
export interface CreateSamlServiceProviderCertificateResponse {
  /** Unique identifier of the SAML service provider */
  samlServiceProviderId: string;
  /** Unique identifier of the SAML service provider certificate */
  samlServiceProviderCertificateId: string;
}

/** Response from {@link SamlResource["getServiceProviderCertificate"] | getServiceProviderCertificate}. */
export interface GetSamlServiceProviderCertificateResponse {
  /** Unique identifier of the SAML service provider certificate */
  samlServiceProviderCertificateId: string;
  /** Unique identifier of the SAML service provider */
  samlServiceProviderId: string;
  /**
   * Purpose of the certificate. Use `signing` for signing SAML requests and `encryption` for encrypting SAML responses.
   */
  purpose: 'signing' | 'encryption';
  /** PEM-encoded X.509 certificate */
  publicKeyCertificate: string;
  /** Whether the certificate is currently active */
  isActive: boolean;
  /** Date and time when the certificate becomes valid */
  notBefore: string;
  /** Date and time when the certificate expires */
  notAfter: string;
  /** When the certificate was created */
  createdAt: string;
  /** Identifier of the user who created the certificate */
  createdBy: string;
  /** When the certificate was last updated */
  updatedAt: string;
  /** Identifier of the user who last updated the certificate */
  updatedBy: string;
}

/** Response from {@link SamlResource["removeServiceProviderCertificate"] | removeServiceProviderCertificate}. */
export type RemoveSamlServiceProviderCertificateResponse = Record<
  string,
  never
>;

/** Response from {@link SamlResource["activateServiceProviderCertificate"] | activateServiceProviderCertificate}. */
export type ActivateSamlServiceProviderCertificateResponse = Record<
  string,
  never
>;

/** Response from {@link SamlResource["deactivateServiceProviderCertificate"] | deactivateServiceProviderCertificate}. */
export type DeactivateSamlServiceProviderCertificateResponse = Record<
  string,
  never
>;

// ---------------------------------------------------------------------------
// SamlResource
// ---------------------------------------------------------------------------

export class SamlResource {
  constructor(private client: Client<paths>) {}

  /**
   * List SAML service providers (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listsamlserviceproviders
   *
   * List all SAML service providers in the organization.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   *
   * @param query - Query parameters
   * @returns ListSamlServiceProvidersResponse
   */
  async listServiceProviders(
    query?: ListSamlServiceProvidersQuery,
  ): Promise<DeepReadonly<ListSamlServiceProvidersResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/saml/service-providers',
      { params: { query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List SAML service providers (Beta) (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listsamlserviceproviders
   *
   * List all SAML service providers in the organization.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   *
   * @param query - Query parameters
   * @yields ListSamlServiceProvidersResponse
   */
  listServiceProvidersAll(
    query?: ListSamlServiceProvidersQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListSamlServiceProvidersResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listServiceProviders({
          ...query,
          pageToken: cursor,
        } as ListSamlServiceProvidersQuery);
        yield page;
        cursor = (page as { nextPageToken?: string | null })[
          'nextPageToken'
        ] as string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * List SAML service provider certificates (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listsamlserviceprovidercertificates
   *
   * List all certificates for a given SAML service provider.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param query - Query parameters
   * @returns ListSamlServiceProviderCertificatesResponse
   */
  async listServiceProviderCertificates(
    samlServiceProviderId: string,
    query?: ListSamlServiceProviderCertificatesQuery,
  ): Promise<DeepReadonly<ListSamlServiceProviderCertificatesResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates',
      { params: { path: { samlServiceProviderId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List SAML service provider certificates (Beta) (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listsamlserviceprovidercertificates
   *
   * List all certificates for a given SAML service provider.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param query - Query parameters
   * @yields ListSamlServiceProviderCertificatesResponse
   */
  listServiceProviderCertificatesAll(
    samlServiceProviderId: string,
    query?: ListSamlServiceProviderCertificatesQuery,
  ): PaginatedAsyncGenerator<
    DeepReadonly<ListSamlServiceProviderCertificatesResponse>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listServiceProviderCertificates(
          samlServiceProviderId,
          {
            ...query,
            pageToken: cursor,
          } as ListSamlServiceProviderCertificatesQuery,
        );
        yield page;
        cursor = (page as { nextPageToken?: string | null })[
          'nextPageToken'
        ] as string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create a SAML service provider certificate (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/createsamlserviceprovidercertificate
   *
   * Create a certificate for a given SAML service provider.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param body - The request body.
   * @returns CreateSamlServiceProviderCertificateResponse
   */
  async createServiceProviderCertificate(
    samlServiceProviderId: string,
    body: CreateSamlServiceProviderCertificateBody,
  ): Promise<DeepReadonly<CreateSamlServiceProviderCertificateResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates',
      { params: { path: { samlServiceProviderId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Get a SAML service provider certificate (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/getsamlserviceprovidercertificate
   *
   * Get a certificate to use with your IdP when configuring Sigma as a service provider.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   * - Retrieve the `samlServiceProviderCertificateId` by calling the [/saml/service-providers/certificates](listsamlspcertificates) endpoint.
   * - Signing certificates are used by your IdP to verify signatures that Sigma attaches to SAML requests.
   * - Encryption certificates are used by your IdP to encrypt SAML responses sent to Sigma.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param samlServiceProviderCertificateId - Unique identifier of the SAML service provider certificate
   * @returns GetSamlServiceProviderCertificateResponse
   */
  async getServiceProviderCertificate(
    samlServiceProviderId: string,
    samlServiceProviderCertificateId: string,
  ): Promise<DeepReadonly<GetSamlServiceProviderCertificateResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates/{samlServiceProviderCertificateId}',
      {
        params: {
          path: { samlServiceProviderId, samlServiceProviderCertificateId },
        },
      },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Remove a SAML service provider certificate (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/removesamlserviceprovidercertificate
   *
   * Remove a certificate.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   * - Retrieve the `samlServiceProviderCertificateId` by calling the [/saml/service-providers/certificates](listsamlspcertificates) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param samlServiceProviderCertificateId - Unique identifier of the SAML service provider certificate
   * @returns RemoveSamlServiceProviderCertificateResponse
   */
  async removeServiceProviderCertificate(
    samlServiceProviderId: string,
    samlServiceProviderCertificateId: string,
  ): Promise<DeepReadonly<RemoveSamlServiceProviderCertificateResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates/{samlServiceProviderCertificateId}',
      {
        params: {
          path: { samlServiceProviderId, samlServiceProviderCertificateId },
        },
      },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Activate a SAML service provider certificate (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/activatesamlserviceprovidercertificate
   *
   * Activate a certificate.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Activating a certificate deactivates all certificates used for the same purpose by the same SAML service provider. For example, activating a signing certificate deactivates all other signing certificates in use by the same SAML SP.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   * - Retrieve the `samlServiceProviderCertificateId` by calling the [/saml/service-providers/certificates](listsamlspcertificates) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param samlServiceProviderCertificateId - Unique identifier of the SAML service provider certificate
   * @param body - The request body.
   * @returns ActivateSamlServiceProviderCertificateResponse
   */
  async activateServiceProviderCertificate(
    samlServiceProviderId: string,
    samlServiceProviderCertificateId: string,
    body: ActivateSamlServiceProviderCertificateBody,
  ): Promise<DeepReadonly<ActivateSamlServiceProviderCertificateResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates/{samlServiceProviderCertificateId}/activate',
      {
        params: {
          path: { samlServiceProviderId, samlServiceProviderCertificateId },
        },
        body,
      },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Deactivate a SAML service provider certificate (Beta)
   *
   * Source docs: https://help.sigmacomputing.com/reference/deactivatesamlserviceprovidercertificate
   *
   * Deactivate a certificate.
   *
   * **Beta**: This documentation describes a public beta feature and is subject to the [Beta features](doc:sigma-product-releases#beta-features) disclaimer.
   *
   * ### Usage notes
   * - To perform this operation, you must use API credentials owned by a user assigned the Admin account type.
   * - Retrieve the `samlServiceProviderId` by calling the [/saml/service-providers](listsamlserviceproviders) endpoint.
   * - Retrieve the `samlServiceProviderCertificateId` by calling the [/saml/service-providers/certificates](listsamlspcertificates) endpoint.
   *
   * @param samlServiceProviderId - Unique identifier of the SAML service provider
   * @param samlServiceProviderCertificateId - Unique identifier of the SAML service provider certificate
   * @param body - The request body.
   * @returns DeactivateSamlServiceProviderCertificateResponse
   */
  async deactivateServiceProviderCertificate(
    samlServiceProviderId: string,
    samlServiceProviderCertificateId: string,
    body: DeactivateSamlServiceProviderCertificateBody,
  ): Promise<DeepReadonly<DeactivateSamlServiceProviderCertificateResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/saml/service-providers/{samlServiceProviderId}/certificates/{samlServiceProviderCertificateId}/deactivate',
      {
        params: {
          path: { samlServiceProviderId, samlServiceProviderCertificateId },
        },
        body,
      },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }
}
