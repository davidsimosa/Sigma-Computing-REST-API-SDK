// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import {
  PaginatedAsyncGenerator,
  DeepReadonly,
  HydratePage,
} from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link WorkspacesResource["list_v2"] | list_v2}. */
export interface List_v2_WorkspacesQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  /** Filter workspaces by name (substring match, case-insensitive). */
  name?: string;
  /**
   * Filter workspaces by exact name match (case-insensitive, like `name`). When set, this takes precedence over `name` and uses an indexed equality lookup instead of a substring search.
   */
  exactName?: string;
}

/** Query parameters for {@link WorkspacesResource["listGrants"] | listGrants}. */
export interface ListWorkspaceGrantsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

/** Query parameters for {@link WorkspacesResource["list"] | list}. */
export interface ListWorkspacesQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  /** Filter workspaces by name (substring match, case-insensitive). */
  name?: string;
  /**
   * Filter workspaces by exact name match (case-insensitive, like `name`). When set, this takes precedence over `name` and uses an indexed equality lookup instead of a substring search.
   */
  exactName?: string;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link WorkspacesResource["create"] | create}. */
export interface CreateWorkspaceBody {
  /** Name of the new workspace. */
  name: string;
  /** Set to true to avoid creating a workspace with a duplicate name. */
  noDuplicates?: boolean;
}

/** Request body for {@link WorkspacesResource["update"] | update}. */
export interface UpdateWorkspaceBody {
  /** Name of the new workspace. */
  name: string;
  /** Set to true to avoid creating a workspace with a duplicate name. */
  noDuplicates?: boolean;
}

/** Request body for {@link WorkspacesResource["createGrant"] | createGrant}. */
export interface CreateWorkspaceGrantBody {
  grants: {
    grantee:
      | {
          memberId: string;
        }
      | {
          teamId: string;
        };
    permission: 'view' | 'explore' | 'organize' | 'edit';
  }[];
}

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

export type List_v2_WorkspacesResponse_SinglePage = {
  workspaceId: string;
  workspaceUrlId: string;
  name: string;
  createdBy: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
}[];

export interface List_v2_WorkspacesResponse_PaginatedResults {
  entries: {
    workspaceId: string;
    workspaceUrlId: string;
    name: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
  }[];
  nextPage: string | null;
}

/** Response from {@link WorkspacesResource["list_v2"] | list_v2}. */
export type List_v2_WorkspacesResponse =
  | List_v2_WorkspacesResponse_SinglePage
  | List_v2_WorkspacesResponse_PaginatedResults;

export interface CreateWorkspaceResponse_WorkspaceDetails {
  workspaceId: string;
  /** Base62-encoded identifier used in Sigma workspace URLs. */
  workspaceUrlId: string;
  name: string;
  createdBy: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
}

export interface CreateWorkspaceResponse_Variant2 {
  workspaceIds: string[];
  workspaceUrlIds: string[];
}

/** Response from {@link WorkspacesResource["create"] | create}. */
export type CreateWorkspaceResponse =
  CreateWorkspaceResponse_WorkspaceDetails | CreateWorkspaceResponse_Variant2;

/** Response from {@link WorkspacesResource["get"] | get}. */
export interface GetWorkspaceResponse {
  workspaceId: string;
  /** Base62-encoded identifier used in Sigma workspace URLs. */
  workspaceUrlId: string;
  name: string;
  createdBy: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
}

/** Response from {@link WorkspacesResource["update"] | update}. */
export interface UpdateWorkspaceResponse {
  workspaceId: string;
  /** Base62-encoded identifier used in Sigma workspace URLs. */
  workspaceUrlId: string;
  name: string;
  createdBy: string;
  updatedBy: string;
  createdAt: string;
  updatedAt: string;
}

/** Response from {@link WorkspacesResource["delete"] | delete}. */
export type DeleteWorkspaceResponse = Record<string, never>;

/** Response from {@link WorkspacesResource["listGrants"] | listGrants}. */
export interface ListWorkspaceGrantsResponse {
  entries: {
    grantId: string;
    inodeId: string;
    organizationId: string;
    memberId: string | null;
    teamId: string | null;
    permission:
      | 'admin'
      | 'annotate'
      | 'update'
      | 'usage'
      | 'writeback'
      | 'create'
      | 'organize'
      | 'explore'
      | 'view'
      | 'edit'
      | 'apply';
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
  }[];
  nextPage: string | null;
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

/** Response from {@link WorkspacesResource["createGrant"] | createGrant}. */
export type CreateWorkspaceGrantResponse = Record<string, never>;

/** Response from {@link WorkspacesResource["deleteGrant"] | deleteGrant}. */
export type DeleteWorkspaceGrantResponse = Record<string, never>;

/** Response from {@link WorkspacesResource["list"] | list}. */
export interface ListWorkspacesResponse {
  /** Array of results returned by the endpoint */
  entries: {
    workspaceId: string;
    workspaceUrlId: string;
    name: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

// ---------------------------------------------------------------------------
// Sub-resource accessors (hydration)
// ---------------------------------------------------------------------------

/** Sub-resource accessors available on hydrated workspace entries. */
export interface WorkspaceSubResources {
  grants: {
    /**
     * List grants for a workspace
     *
     * Source docs: https://help.sigmacomputing.com/reference/listworkspacegrants
     *
     * This endpoint retrieves a list of all permission grants associated with a specific workspace, identified by its workspaceId.
     *
     * ### Usage notes
     * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
     * - Optionally use the `page` and `limit` parameters to manage the size of the returned data and navigate through large sets of data.
     *
     * ### Usage scenarios
     * - **Access control management**: Useful for administrators to review and manage who has what access to the workspace.
     * - **Audit and compliance**: Assists in compliance checks and audits by providing a clear view of the permissions granted to different users and teams.
     *
     * ### Best practices
     * - **Use pagination**: Especially important for workspaces with a large number of grants to ensure performance and usability.
     * - **Regular review**: Regularly review and update the workspace grants to ensure they align with current organizational policies and user roles.
     *
     * @param query - Query parameters
     * @returns ListWorkspaceGrantsResponse
     */
    list(
      query?: ListWorkspaceGrantsQuery,
    ): Promise<DeepReadonly<ListWorkspaceGrantsResponse>>;
    /**
     * List grants for a workspace (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listworkspacegrants
     *
     * This endpoint retrieves a list of all permission grants associated with a specific workspace, identified by its workspaceId.
     *
     * ### Usage notes
     * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
     * - Optionally use the `page` and `limit` parameters to manage the size of the returned data and navigate through large sets of data.
     *
     * ### Usage scenarios
     * - **Access control management**: Useful for administrators to review and manage who has what access to the workspace.
     * - **Audit and compliance**: Assists in compliance checks and audits by providing a clear view of the permissions granted to different users and teams.
     *
     * ### Best practices
     * - **Use pagination**: Especially important for workspaces with a large number of grants to ensure performance and usability.
     * - **Regular review**: Regularly review and update the workspace grants to ensure they align with current organizational policies and user roles.
     *
     * @param query - Query parameters
     * @yields ListWorkspaceGrantsResponse
     */
    listAll(
      query?: ListWorkspaceGrantsQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListWorkspaceGrantsResponse>>;
    /**
     * Grant permissions to the workspace for users or teams
     *
     * Source docs: https://help.sigmacomputing.com/reference/createworkspacegrant
     *
     * This endpoint allows creating a grant which specifies permissions for one or more users or teams to access a workspace.
     *
     * ### Usage notes
     * - **Grants**: An array of grant objects. Each object specifies a grantee and the permission level assigned to them.
     * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
     * - Do not set both **memberId** and **teamId** in the same **grantee** object. Instead, choose one based on the target of the grant.
     *
     *   - Retrieve the **memberId** by calling the [/v2/members](listmembers) endpoint.
     *   - Retrieve the **teamId** by calling the [/v2/teams](listteams) endpoint.
     *
     * ### Usage scenarios
     * - **Access control setup**: Use this endpoint to set up or modify access controls for a workspace, specifying which users or teams can access and what actions they can perform.
     *
     * ### Best practices
     * - **Clear permission definitions**: Ensure that the permissions granted are well understood by both the grantor and the grantee to avoid misuse of access rights.
     *
     * @param body - The request body.
     * @returns CreateWorkspaceGrantResponse
     */
    create(
      body: CreateWorkspaceGrantBody,
    ): Promise<DeepReadonly<CreateWorkspaceGrantResponse>>;
  };
}

/** A workspace entry with sub-resource accessor methods. Response data properties are deeply readonly. */
export type HydratedWorkspace<T> = DeepReadonly<T> & WorkspaceSubResources;

// ---------------------------------------------------------------------------
// WorkspacesResource
// ---------------------------------------------------------------------------

/** @internal Attach sub-resource accessors to a workspace entry. */
function _hydrateWorkspace<T extends object>(
  entry: T,
  resource: WorkspacesResource,
): HydratedWorkspace<T> {
  return Object.assign(entry, {
    grants: {
      list: (query?: ListWorkspaceGrantsQuery) =>
        resource.listGrants((entry as any).workspaceId, query),
      listAll: (query?: ListWorkspaceGrantsQuery) =>
        resource.listGrantsAll((entry as any).workspaceId, query),
      create: (body: CreateWorkspaceGrantBody) =>
        resource.createGrant((entry as any).workspaceId, body),
    },
  }) as HydratedWorkspace<T>;
}

export class WorkspacesResource {
  constructor(private client: Client<paths>) {}

  /**
   * List workspaces
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkspaces
   *
   *
   *   **Attention**: This endpoint will return only paginated responses starting June 2, 2026. To start returning paginated responses before that date, include the query parameter `limit` in your request.
   *
   *   This endpoint returns a list of all workspaces. You can use pagination and optionally filter by name to manage large sets of data. See [Manage Workspaces](doc:manage-workspaces) for more details about workspaces in Sigma.
   *   ### Usage notes
   *   - **Filtering by name**: Optionally, use the `name` parameter to filter workspaces by a case-insensitive substring match.
   *   - **Filtering by exact name**: Optionally, use the `exactName` parameter to filter workspaces by an exact name match (also case-insensitive). When provided, `exactName` takes precedence over `name` and uses an indexed equality lookup, which is significantly faster than substring search.
   *
   *   ### Usage scenarios
   *   - **Pagination**: Use the `page` and `limit` parameters to control the size and segment of the workspace list returned.
   *   - **User interface display**: Populate a user interface with a list of all available workspaces, using pagination to efficiently load data and filtering to quickly find specific workspaces.
   *   - **Administrative overview**: Provide system administrators with an overview of all workspaces for management and monitoring purposes, with the ability to browse through pages and search by name.
   *
   *   ### Best practices
   *   - Use caching to reduce load times and server demand when frequently accessing workspace lists.
   *   - Regularly update and synchronize workspace lists to ensure that displayed information is current and accurate.
   *
   * @param query - Query parameters
   * @returns List_v2_WorkspacesResponse
   */
  async list_v2(
    query?: List_v2_WorkspacesQuery,
  ): Promise<DeepReadonly<List_v2_WorkspacesResponse>> {
    const { data, error, response } = await this.client.GET('/v2/workspaces', {
      params: { query },
    });
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List workspaces (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkspaces
   *
   *
   *   **Attention**: This endpoint will return only paginated responses starting June 2, 2026. To start returning paginated responses before that date, include the query parameter `limit` in your request.
   *
   *   This endpoint returns a list of all workspaces. You can use pagination and optionally filter by name to manage large sets of data. See [Manage Workspaces](doc:manage-workspaces) for more details about workspaces in Sigma.
   *   ### Usage notes
   *   - **Filtering by name**: Optionally, use the `name` parameter to filter workspaces by a case-insensitive substring match.
   *   - **Filtering by exact name**: Optionally, use the `exactName` parameter to filter workspaces by an exact name match (also case-insensitive). When provided, `exactName` takes precedence over `name` and uses an indexed equality lookup, which is significantly faster than substring search.
   *
   *   ### Usage scenarios
   *   - **Pagination**: Use the `page` and `limit` parameters to control the size and segment of the workspace list returned.
   *   - **User interface display**: Populate a user interface with a list of all available workspaces, using pagination to efficiently load data and filtering to quickly find specific workspaces.
   *   - **Administrative overview**: Provide system administrators with an overview of all workspaces for management and monitoring purposes, with the ability to browse through pages and search by name.
   *
   *   ### Best practices
   *   - Use caching to reduce load times and server demand when frequently accessing workspace lists.
   *   - Regularly update and synchronize workspace lists to ensure that displayed information is current and accurate.
   *
   * @param query - Query parameters
   * @yields List_v2_WorkspacesResponse
   */
  list_v2All(
    query?: List_v2_WorkspacesQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<List_v2_WorkspacesResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list_v2({
          ...query,
          page: cursor,
        } as List_v2_WorkspacesQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create a workspace
   *
   * Source docs: https://help.sigmacomputing.com/reference/createworkspace
   *
   * This endpoint allows clients to create a workspace with specific characteristics.
   *
   * ### Usage notes
   * - Set **NoDuplicates** to **true** to prevent the creation of a workspace with a name that already exists.
   *
   * ### Usage scenarios
   * - **Initial setup**: Useful for users setting up a new workspace after signing up.
   * - **Project separation**: Helps in creating separate workspaces for different projects or teams.
   *
   * ### Best practices
   * - **Check for existing names**: Before setting `noDuplicates` to true, make sure to search for existing workspace names to avoid conflicts.
   * - **Consistent naming conventions**: Adopt a consistent naming convention for workspaces to ensure clarity and avoid confusion.
   *
   * @param body - The request body.
   * @returns CreateWorkspaceResponse
   */
  async create(
    body: CreateWorkspaceBody,
  ): Promise<HydratedWorkspace<CreateWorkspaceResponse_WorkspaceDetails>> {
    const { data, error, response } = await this.client.POST('/v2/workspaces', {
      body,
    });
    if (error) throw new SigmaApiError(error, response);
    // NOTE: return type is narrowed from CreateWorkspaceResponse to CreateWorkspaceResponse_WorkspaceDetails.
    // The spec declares a oneOf response; the full union collapses sub-resource
    // accessors when intersected. Re-run `npm run generate` if the spec is fixed.
    return _hydrateWorkspace(data! as any, this);
  }

  /**
   * Get a workspace
   *
   * Source docs: https://help.sigmacomputing.com/reference/getworkspace
   *
   * This endpoint retrieves the details of a specific workspace by its workspaceId.
   *
   * ### Usage notes
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   *
   * ### Usage scenarios
   * - **Workspace management**: Useful for administrators or users who need to view the details of a specific workspace.
   * - **Integration checks**: Can be used by external systems to verify the existence and status of a workspace as part of integration workflows.
   *
   * ### Best practices
   * - **Validate workspaceId**: Ensure the workspaceId provided is valid and corresponds to an existing workspace. Handle any errors gracefully.
   * - **Access controls**: Implement proper authorization checks to ensure that only entitled users can access workspace details.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @returns GetWorkspaceResponse
   */
  async get(
    workspaceId: string,
  ): Promise<HydratedWorkspace<GetWorkspaceResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/workspaces/{workspaceId}',
      { params: { path: { workspaceId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return _hydrateWorkspace(data! as any, this);
  }

  /**
   * Update a workspace
   *
   * Source docs: https://help.sigmacomputing.com/reference/updateworkspace
   *
   * This endpoint updates the name of an existing workspace identified by its workspaceId.
   *
   * ### Usage notes
   * - Set `NoDuplicates` to `true` to prevent the creation of a duplicate workspace.
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   *
   * ### Usage scenarios
   * - **Configuration change**: Allows users to update workspace settings or names as projects evolve or requirements change.
   * - **Access control adjustments**: Update workspace details in response to organizational changes or policy updates.
   *
   * ### Best practices
   * - **Partial updates**: Use PATCH to support partial updates, only sending the fields that need to be updated.
   * - **Validation**: Validate input data to ensure it adheres to expected formats and constraints. Handle errors gracefully and inform the user of any constraints.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @param body - The request body.
   * @returns UpdateWorkspaceResponse
   */
  async update(
    workspaceId: string,
    body: UpdateWorkspaceBody,
  ): Promise<HydratedWorkspace<UpdateWorkspaceResponse>> {
    const { data, error, response } = await this.client.PATCH(
      '/v2/workspaces/{workspaceId}',
      { params: { path: { workspaceId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return _hydrateWorkspace(data! as any, this);
  }

  /**
   * Delete a workspace
   *
   * Source docs: https://help.sigmacomputing.com/reference/deleteworkspace
   *
   * You can use this endpoint to delete an existing workspace by its workspaceId. **Caution:** Deleted workspaces cannot be recovered.
   *
   * ### Usage notes
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   *
   * ### Usage scenarios
   * - **Cleanup operations**: Useful for removing workspaces that are no longer needed or relevant.
   * - **Resource management**: Helps in managing the overall resource allocation by removing unused workspaces.
   *
   * ### Best practices
   * - **Confirm before deletion**: Always ensure that deletion operations are preceded by explicit user confirmations to prevent accidental loss of data.
   * - **Audit and logging**: Maintain audit logs for deletion actions to track who deleted the workspace and when.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @returns DeleteWorkspaceResponse
   */
  async delete(
    workspaceId: string,
  ): Promise<DeepReadonly<DeleteWorkspaceResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/workspaces/{workspaceId}',
      { params: { path: { workspaceId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List grants for a workspace
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkspacegrants
   *
   * This endpoint retrieves a list of all permission grants associated with a specific workspace, identified by its workspaceId.
   *
   * ### Usage notes
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   * - Optionally use the `page` and `limit` parameters to manage the size of the returned data and navigate through large sets of data.
   *
   * ### Usage scenarios
   * - **Access control management**: Useful for administrators to review and manage who has what access to the workspace.
   * - **Audit and compliance**: Assists in compliance checks and audits by providing a clear view of the permissions granted to different users and teams.
   *
   * ### Best practices
   * - **Use pagination**: Especially important for workspaces with a large number of grants to ensure performance and usability.
   * - **Regular review**: Regularly review and update the workspace grants to ensure they align with current organizational policies and user roles.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @param query - Query parameters
   * @returns ListWorkspaceGrantsResponse
   */
  async listGrants(
    workspaceId: string,
    query?: ListWorkspaceGrantsQuery,
  ): Promise<DeepReadonly<ListWorkspaceGrantsResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/workspaces/{workspaceId}/grants',
      { params: { path: { workspaceId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List grants for a workspace (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listworkspacegrants
   *
   * This endpoint retrieves a list of all permission grants associated with a specific workspace, identified by its workspaceId.
   *
   * ### Usage notes
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   * - Optionally use the `page` and `limit` parameters to manage the size of the returned data and navigate through large sets of data.
   *
   * ### Usage scenarios
   * - **Access control management**: Useful for administrators to review and manage who has what access to the workspace.
   * - **Audit and compliance**: Assists in compliance checks and audits by providing a clear view of the permissions granted to different users and teams.
   *
   * ### Best practices
   * - **Use pagination**: Especially important for workspaces with a large number of grants to ensure performance and usability.
   * - **Regular review**: Regularly review and update the workspace grants to ensure they align with current organizational policies and user roles.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @param query - Query parameters
   * @yields ListWorkspaceGrantsResponse
   */
  listGrantsAll(
    workspaceId: string,
    query?: ListWorkspaceGrantsQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListWorkspaceGrantsResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listGrants(workspaceId, {
          ...query,
          page: cursor,
        } as ListWorkspaceGrantsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Grant permissions to the workspace for users or teams
   *
   * Source docs: https://help.sigmacomputing.com/reference/createworkspacegrant
   *
   * This endpoint allows creating a grant which specifies permissions for one or more users or teams to access a workspace.
   *
   * ### Usage notes
   * - **Grants**: An array of grant objects. Each object specifies a grantee and the permission level assigned to them.
   * - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   * - Do not set both **memberId** and **teamId** in the same **grantee** object. Instead, choose one based on the target of the grant.
   *
   *   - Retrieve the **memberId** by calling the [/v2/members](listmembers) endpoint.
   *   - Retrieve the **teamId** by calling the [/v2/teams](listteams) endpoint.
   *
   * ### Usage scenarios
   * - **Access control setup**: Use this endpoint to set up or modify access controls for a workspace, specifying which users or teams can access and what actions they can perform.
   *
   * ### Best practices
   * - **Clear permission definitions**: Ensure that the permissions granted are well understood by both the grantor and the grantee to avoid misuse of access rights.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @param body - The request body.
   * @returns CreateWorkspaceGrantResponse
   */
  async createGrant(
    workspaceId: string,
    body: CreateWorkspaceGrantBody,
  ): Promise<DeepReadonly<CreateWorkspaceGrantResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/workspaces/{workspaceId}/grants',
      { params: { path: { workspaceId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Delete a workspace grant
   *
   * Source docs: https://help.sigmacomputing.com/reference/deleteworkspacegrant
   *
   * Delete a specific grant of permissions granted on a workspace to a user or team.
   *
   *   ### Usage notes
   *   - Retrieve the **workspaceId** by calling the [/v2/workspaces](listworkspaces) endpoint.
   *   - Retrieve the **grantId** by calling the [/v2/workspaces/{workspaceId}/grants](listworkspacegrants) endpoint.
   *
   * @param workspaceId - Unique identifier of the workspace.
   * @param grantId
   * @returns DeleteWorkspaceGrantResponse
   */
  async deleteGrant(
    workspaceId: string,
    grantId: string,
  ): Promise<DeepReadonly<DeleteWorkspaceGrantResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/workspaces/{workspaceId}/grants/{grantId}',
      { params: { path: { workspaceId, grantId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List workspaces (Paginated)
   *
   * Source docs: https://help.sigmacomputing.com/reference/v2_1_listworkspaces
   *
   *
   *   **Attention:** This API endpoint uses pagination by default.
   *
   *   This endpoint returns all workspaces. See [Manage Workspaces](doc:manage-workspaces) for more details about workspaces in Sigma.
   *
   *   ### Usage notes
   *   - **Pagination**: Use the `page` and `limit` parameters to control the size and segment of the workspace list returned.
   *   - **Filtering by name**: Optionally, use the `name` parameter to filter workspaces by a case-insensitive substring match.
   *   - **Filtering by exact name**: Optionally, use the `exactName` parameter to filter workspaces by an exact name match (also case-insensitive). When provided, `exactName` takes precedence over `name` and uses an indexed equality lookup, which is significantly faster than substring search.
   *
   *   ### Usage scenarios
   *   - **User interface display**: Populate a user interface with a list of all available workspaces, using pagination to efficiently load data and filtering to quickly find specific workspaces.
   *   - **Administrative overview**: Provide system administrators with an overview of all workspaces for management and monitoring purposes, with the ability to browse through pages and search by name.
   *
   *   ### Best practices
   *   - Use caching to reduce load times and server demand when frequently accessing workspace lists.
   *   - Regularly update and synchronize workspace lists to ensure that displayed information is current and accurate.
   *
   * @param query - Query parameters
   * @returns ListWorkspacesResponse
   */
  async list(
    query?: ListWorkspacesQuery,
  ): Promise<HydratePage<ListWorkspacesResponse, WorkspaceSubResources>> {
    const { data, error, response } = await this.client.GET(
      '/v2.1/workspaces',
      { params: { query } },
    );
    if (error) throw new SigmaApiError(error, response);
    const result = data! as any;
    if (Array.isArray(result)) {
      return result.map((e: any) => _hydrateWorkspace(e, this)) as any;
    }
    if (result.entries && Array.isArray(result.entries)) {
      result.entries = result.entries.map((e: any) =>
        _hydrateWorkspace(e, this),
      );
    }
    return result;
  }

  /**
   * List workspaces (Paginated) (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/v2_1_listworkspaces
   *
   *
   *   **Attention:** This API endpoint uses pagination by default.
   *
   *   This endpoint returns all workspaces. See [Manage Workspaces](doc:manage-workspaces) for more details about workspaces in Sigma.
   *
   *   ### Usage notes
   *   - **Pagination**: Use the `page` and `limit` parameters to control the size and segment of the workspace list returned.
   *   - **Filtering by name**: Optionally, use the `name` parameter to filter workspaces by a case-insensitive substring match.
   *   - **Filtering by exact name**: Optionally, use the `exactName` parameter to filter workspaces by an exact name match (also case-insensitive). When provided, `exactName` takes precedence over `name` and uses an indexed equality lookup, which is significantly faster than substring search.
   *
   *   ### Usage scenarios
   *   - **User interface display**: Populate a user interface with a list of all available workspaces, using pagination to efficiently load data and filtering to quickly find specific workspaces.
   *   - **Administrative overview**: Provide system administrators with an overview of all workspaces for management and monitoring purposes, with the ability to browse through pages and search by name.
   *
   *   ### Best practices
   *   - Use caching to reduce load times and server demand when frequently accessing workspace lists.
   *   - Regularly update and synchronize workspace lists to ensure that displayed information is current and accurate.
   *
   * @param query - Query parameters
   * @yields ListWorkspacesResponse
   */
  listAll(
    query?: ListWorkspacesQuery,
  ): PaginatedAsyncGenerator<
    HydratePage<ListWorkspacesResponse, WorkspaceSubResources>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list({
          ...query,
          page: cursor,
        } as ListWorkspacesQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }
}
