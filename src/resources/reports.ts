// AUTO-GENERATED by scripts/generate-resources.ts -- DO NOT EDIT
import type { Client } from 'openapi-fetch';
import type { paths, operations, components } from '../generated/openapi';
import { SigmaApiError } from '../errors';
import {
  PaginatedAsyncGenerator,
  DeepReadonly,
  HydratePage,
} from '../pagination';

// ---------------------------------------------------------------------------
// Query parameter interfaces
// ---------------------------------------------------------------------------

/** Query parameters for {@link ReportsResource["list"] | list}. */
export interface ListReportsQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
  excludeTags?: boolean;
  /** Only available to Sigma admins. Return all reports regardless of whether the user has access. */
  skipPermissionCheck?: boolean;
  /** Return archived reports. */
  isArchived?: boolean;
}

/** Query parameters for {@link ReportsResource["listSchedules"] | listSchedules}. */
export interface ListReportSchedulesQuery {
  /** Use to specify further pages using the string returned in the nextPage portion of the response. */
  page?: string;
  /**
   * Number of results to return per page, with a maximum of 1,000 per request. Use the `page` option in subsequent requests to retrieve the next set of results.
   */
  limit?: number;
}

// ---------------------------------------------------------------------------
// Request body types
// ---------------------------------------------------------------------------

/** Request body for {@link ReportsResource["create"] | create}. */
export interface CreateReportBody {
  /** ID of the folder where the new report is created. */
  folderId: string;
  /** Name of the new report. */
  name: string;
  /** Description of the report. */
  description?: string;
}

/** Request body for {@link ReportsResource["copy"] | copy}. */
export interface CopyReportBody {
  /** ID of the folder in which to place the copied report. */
  destinationFolderId: string;
  /** Name for the copied report as it appears in Sigma. */
  name: string;
  /** A description for the new report. */
  description?: string;
}

/** Request body for {@link ReportsResource["export"] | export}. */
export interface ExportReportBody {
  format: {
    type: 'pdf';
    layout: 'portrait' | 'landscape';
  };
  /**
   * Specify the control ID and control value of one or more control elements in the workbook or report to filter the returned data. The control must exist in the Sigma workbook or report and target an element before you can export based on the control value.
   *
   * **Syntax**
   * { control-id: control-value }
   *
   * The control-id is configured in the Sigma workbook or report. The control-value is the value that is passed to the control.
   *
   * Values can take multiple forms, depending on the data type of the control:
   *
   * **Boolean**
   *
   * - Single value: "true" | "false" | ":null"
   * - List of Boolean: "true,false,:null" (No space after the comma.)
   *
   * **Number**
   *
   * - Single value: "10.54", ":null"
   * - List of values: "10.54,23.45,:null" (No space after the comma.)
   * - Range of numbers: "min:10.54,max:23.45" (No space after the comma or colon.)
   *
   * **Text**
   *
   * - Single value: "some-text" | ":null" | ":empty"
   * - List of values: "some-text,more-text,:null,:empty" (No space after the comma.)
   *
   * **Date**
   *
   * All dates use UTC time zones.
   *
   * - Fixed date using ISO-6801 format: "2022-01-01T01:01:59" | ":null"
   *     - Formats supported (where: %Y: year, %m: month, %d: day, %H:hours, %M: minutes, %S: seconds):
   *         - %Y-%m-%d
   *         - %Y-%m-%dT%H:%M
   *         - %Y-%m-%dT%H:%M:%S
   * - List of fixed dates: "2022-01-01T01:01:59,2022-02-02T02:02:59,:null"
   * - Relative date: "prior-day-3" | "next-day-3"
   *     - Format: [prior|next]-[year|quarter|month|week|isoWeek|day|hour|minute]-[number]
   * - Date range: "min:2022-01-01T01:01:59,max:next-day-3" (No spaces after the comma or colon.)
   *     - Leave max blank for "on or after" and min blank for "on or before", for example:
   *         - "Date-Range": "min:2020-04-01,max:"
   *         - "Date-Range": "min:,max:next-day-3"
   *
   * **Notes**
   *
   * - ":null" is a special value that denotes the null primitive value.
   * - ":empty" is a special value that denotes the empty string "".
   *
   * Use [URL encoding](https://help.sigmacomputing.com/docs/special-characters-for-url-parameters) to encode characters in your control values (such as commas) to prevent the system from interpreting them as separators.
   *
   * For example:
   * "New York, NY,Boston, MA" can be encoded as:
   *
   * "New%20York%2C%20NY,Boston%2C%20MA"
   */
  parameters?: Record<string, string>;
  timeout?: number;
  /** Specifies the version tag of the report to be exported. */
  tag?: string;
}

/** Request body for {@link ReportsResource["createSchedule"] | createSchedule}. */
export type CreateReportScheduleBody = NonNullable<
  operations['createReportSchedule']['requestBody']
>['content']['application/json'];

/** Request body for {@link ReportsResource["updateSchedule"] | updateSchedule}. */
export type UpdateReportScheduleBody = NonNullable<
  operations['updateReportSchedule']['requestBody']
>['content']['application/json'];

/** Request body for {@link ReportsResource["send"] | send}. */
export type SendReportBody = NonNullable<
  operations['sendReport']['requestBody']
>['content']['application/json'];

// ---------------------------------------------------------------------------
// Response types
// ---------------------------------------------------------------------------

/** Response from {@link ReportsResource["list"] | list}. */
export interface ListReportsResponse {
  /** Array of results returned by the endpoint */
  entries: {
    reportId: string;
    reportUrlId: string;
    name: string;
    url: string;
    path: string;
    latestVersion: number;
    ownerId: string;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
    isArchived?: boolean;
    tags?: {
      versionTagId: string;
      tagName: string;
      sourceVersion: number;
      taggedAt: string;
      taggedReportId: string;
    }[];
    description?: string;
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

/** Response from {@link ReportsResource["create"] | create}. */
export interface CreateReportResponse {
  /** Unique identifier of the report. */
  reportId: string;
  reportUrlId: string;
  name: string;
  url: string;
  path: string;
  latestVersion: number;
  ownerId: string;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  isArchived?: boolean;
  tags?: {
    versionTagId: string;
    tagName: string;
    sourceVersion: number;
    taggedAt: string;
    taggedReportId: string;
  }[];
  description?: string;
}

/** Response from {@link ReportsResource["get"] | get}. */
export interface GetReportResponse {
  /** Unique identifier of the report. */
  reportId: string;
  reportUrlId: string;
  name: string;
  url: string;
  path: string;
  latestVersion: number;
  ownerId: string;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  isArchived?: boolean;
  tags?: {
    versionTagId: string;
    tagName: string;
    sourceVersion: number;
    taggedAt: string;
    taggedReportId: string;
  }[];
  description?: string;
}

/** Response from {@link ReportsResource["copy"] | copy}. */
export interface CopyReportResponse {
  /** Unique identifier of the report. */
  reportId: string;
  reportUrlId: string;
  name: string;
  url: string;
  path: string;
  latestVersion: number;
  ownerId: string;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
  isArchived?: boolean;
  tags?: {
    versionTagId: string;
    tagName: string;
    sourceVersion: number;
    taggedAt: string;
    taggedReportId: string;
  }[];
  description?: string;
}

/** Response from {@link ReportsResource["export"] | export}. */
export interface ExportReportResponse {
  jobComplete: boolean;
  queryId: string;
  rows?: Record<string, unknown>[];
  message?: string;
}

/** Response from {@link ReportsResource["listSchedules"] | listSchedules}. */
export interface ListReportSchedulesResponse {
  /** Array of results returned by the endpoint */
  entries: {
    scheduledNotificationId: string;
    reportId: string;
    schedule: {
      cronSpec: string;
      timezone: string;
    };
    configV2: {
      title: string;
      messageBody: string;
      notificationAttachments: {
        formatOptions:
          | {
              type: 'PDF';
              layout?: 'portrait' | 'landscape';
              scaleFactor?: number;
            }
          | {
              type: 'NONE';
            };
        workbookExportSource?: {
          type: 'report';
        };
      }[];
      includeLink?: boolean;
      runAsRecipient?: boolean;
      attachmentSettings?: {
        mergePdfAttachments?: boolean;
        zipAttachments?: boolean;
      };
      conditionOptions?:
        | {
            type: 'always';
          }
        | {
            type: 'workbook';
            conditions: (
              | {
                  type: 'isEmpty';
                  sheetId: string;
                }
              | {
                  type: 'isNotEmpty';
                  sheetId: string;
                }
              | {
                  type: 'sheetColumn';
                  sheetId: string;
                  mode: 'any' | 'all';
                  columnId: string;
                  op:
                    | 'Equals'
                    | 'GreaterThan'
                    | 'GreaterThanEq'
                    | 'LessThan'
                    | 'LessThanEq'
                    | 'NotEquals'
                    | 'IsNotNull'
                    | 'IsNull'
                    | 'Contains'
                    | 'StartsWith'
                    | 'EndsWith';
                  value?:
                    | {
                        type: 'null';
                      }
                    | {
                        type: 'bool';
                        val: boolean;
                      }
                    | {
                        type: 'number';
                        val: number;
                      }
                    | {
                        type: 'integer';
                        val: string;
                      }
                    | {
                        type: 'string';
                        val: string;
                      }
                    | {
                        type: 'datetime';
                        val: string;
                      }
                    | {
                        type: 'error';
                        val: string;
                      }
                    | {
                        type: 'record';
                        val: Record<string, unknown>;
                      }
                    | {
                        type: 'array';
                        val:
                          | {
                              type: 'boolArr';
                              data: (boolean | null)[];
                            }
                          | {
                              type: 'numberArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bigintArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'stringArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'datetimeArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bytesArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'variantArr';
                              data: (string | null)[];
                            };
                      }
                    | {
                        type: 'variant';
                        val: string;
                      };
                }
            )[];
          }
        | {
            type: 'worksheet';
            conditions: (
              | {
                  type: 'isEmpty';
                }
              | {
                  type: 'isNotEmpty';
                }
              | {
                  type: 'queryColumn';
                  mode: 'any' | 'all';
                  columnId: string;
                  op:
                    | 'Equals'
                    | 'GreaterThan'
                    | 'GreaterThanEq'
                    | 'LessThan'
                    | 'LessThanEq'
                    | 'NotEquals'
                    | 'IsNotNull'
                    | 'IsNull'
                    | 'Contains'
                    | 'StartsWith'
                    | 'EndsWith';
                  value?:
                    | {
                        type: 'null';
                      }
                    | {
                        type: 'bool';
                        val: boolean;
                      }
                    | {
                        type: 'number';
                        val: number;
                      }
                    | {
                        type: 'integer';
                        val: string;
                      }
                    | {
                        type: 'string';
                        val: string;
                      }
                    | {
                        type: 'datetime';
                        val: string;
                      }
                    | {
                        type: 'error';
                        val: string;
                      }
                    | {
                        type: 'record';
                        val: Record<string, unknown>;
                      }
                    | {
                        type: 'array';
                        val:
                          | {
                              type: 'boolArr';
                              data: (boolean | null)[];
                            }
                          | {
                              type: 'numberArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bigintArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'stringArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'datetimeArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bytesArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'variantArr';
                              data: (string | null)[];
                            };
                      }
                    | {
                        type: 'variant';
                        val: string;
                      };
                }
            )[];
          }
        | {
            type: 'dashboard';
            conditions: (
              | {
                  type: 'isEmpty';
                  tileId: string;
                }
              | {
                  type: 'isNotEmpty';
                  tileId: string;
                }
              | {
                  type: 'chartColumn';
                  tileId: string;
                  mode: 'any' | 'all';
                  columnId: string;
                  op:
                    | 'Equals'
                    | 'GreaterThan'
                    | 'GreaterThanEq'
                    | 'LessThan'
                    | 'LessThanEq'
                    | 'NotEquals'
                    | 'IsNotNull'
                    | 'IsNull'
                    | 'Contains'
                    | 'StartsWith'
                    | 'EndsWith';
                  value?:
                    | {
                        type: 'null';
                      }
                    | {
                        type: 'bool';
                        val: boolean;
                      }
                    | {
                        type: 'number';
                        val: number;
                      }
                    | {
                        type: 'integer';
                        val: string;
                      }
                    | {
                        type: 'string';
                        val: string;
                      }
                    | {
                        type: 'datetime';
                        val: string;
                      }
                    | {
                        type: 'error';
                        val: string;
                      }
                    | {
                        type: 'record';
                        val: Record<string, unknown>;
                      }
                    | {
                        type: 'array';
                        val:
                          | {
                              type: 'boolArr';
                              data: (boolean | null)[];
                            }
                          | {
                              type: 'numberArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bigintArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'stringArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'datetimeArr';
                              data: (number | null)[];
                            }
                          | {
                              type: 'bytesArr';
                              data: (string | null)[];
                            }
                          | {
                              type: 'variantArr';
                              data: (string | null)[];
                            };
                      }
                    | {
                        type: 'variant';
                        val: string;
                      };
                }
            )[];
          };
      alertConfig?: {
        window: 'alltime' | 'daily' | 'weekly' | 'always';
        volume?: number | null;
      };
      notificationName?: string;
      reportSettings?: {
        filters?: {
          id: string;
          filterStr: string;
        }[];
        variableValues?: components['schemas']['DashVariables'];
      };
      dynamicExportSettings?: {
        variables?: Record<
          string,
          | {
              type: 'boolean';
            }
          | {
              type: 'number';
            }
          | {
              type: 'text';
            }
          | {
              type: 'date';
            }
          | {
              type: 'number-list';
            }
          | {
              type: 'text-list';
            }
          | {
              type: 'date-list';
            }
          | {
              type: 'boolean-list';
            }
          | {
              type: 'date-range';
            }
          | {
              type: 'number-range';
            }
          | {
              type: 'date-null';
            }
          | {
              type: 'number-null';
            }
          | {
              type: 'text-null';
            }
          | {
              type: 'boolean-null';
            }
        >;
      };
      dynamicRecipientSettings?: {
        sourceNodeId: string;
        recipientColumnId: string;
      };
      rowLimit?: number;
      reportVariant?: {
        tagId?: string;
      };
      embedOpts?: {
        embedId?: string;
        evalConnectionId?: string;
      };
    };
    isSuspended: boolean;
    ownerId: string;
    lastUpdatedByUser: string;
    disabledAt: string | null;
    disabledBy: string | null;
    createdBy: string;
    updatedBy: string;
    createdAt: string;
    updatedAt: string;
  }[];
  /**
   * Returns a string that can be used as the `page` parameter in the next request to fetch the next page of results. The last page of results returns `null`.
   */
  nextPage: string | null;
  /** Total number of results. Useful to determine if there is a need to paginate. */
  total?: number;
  /** **[Deprecated]** Indicates whether more results are available. */
  hasMore?: boolean;
}

/** Response from {@link ReportsResource["createSchedule"] | createSchedule}. */
export interface CreateReportScheduleResponse {
  /** Unique identifier of the scheduled export. */
  scheduledNotificationId: string;
  /** Unique identifier of the report. */
  reportId: string;
  schedule: {
    cronSpec: string;
    timezone: string;
  };
  configV2: {
    title: string;
    messageBody: string;
    notificationAttachments: {
      formatOptions:
        | {
            type: 'PDF';
            layout?: 'portrait' | 'landscape';
            scaleFactor?: number;
          }
        | {
            type: 'NONE';
          };
      workbookExportSource?: {
        type: 'report';
      };
    }[];
    includeLink?: boolean;
    runAsRecipient?: boolean;
    attachmentSettings?: {
      mergePdfAttachments?: boolean;
      zipAttachments?: boolean;
    };
    conditionOptions?:
      | {
          type: 'always';
        }
      | {
          type: 'workbook';
          conditions: (
            | {
                type: 'isEmpty';
                sheetId: string;
              }
            | {
                type: 'isNotEmpty';
                sheetId: string;
              }
            | {
                type: 'sheetColumn';
                sheetId: string;
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        }
      | {
          type: 'worksheet';
          conditions: (
            | {
                type: 'isEmpty';
              }
            | {
                type: 'isNotEmpty';
              }
            | {
                type: 'queryColumn';
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        }
      | {
          type: 'dashboard';
          conditions: (
            | {
                type: 'isEmpty';
                tileId: string;
              }
            | {
                type: 'isNotEmpty';
                tileId: string;
              }
            | {
                type: 'chartColumn';
                tileId: string;
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        };
    alertConfig?: {
      window: 'alltime' | 'daily' | 'weekly' | 'always';
      volume?: number | null;
    };
    notificationName?: string;
    reportSettings?: {
      filters?: {
        id: string;
        filterStr: string;
      }[];
      variableValues?: components['schemas']['DashVariables'];
    };
    dynamicExportSettings?: {
      variables?: Record<
        string,
        | {
            type: 'boolean';
          }
        | {
            type: 'number';
          }
        | {
            type: 'text';
          }
        | {
            type: 'date';
          }
        | {
            type: 'number-list';
          }
        | {
            type: 'text-list';
          }
        | {
            type: 'date-list';
          }
        | {
            type: 'boolean-list';
          }
        | {
            type: 'date-range';
          }
        | {
            type: 'number-range';
          }
        | {
            type: 'date-null';
          }
        | {
            type: 'number-null';
          }
        | {
            type: 'text-null';
          }
        | {
            type: 'boolean-null';
          }
      >;
    };
    dynamicRecipientSettings?: {
      sourceNodeId: string;
      recipientColumnId: string;
    };
    rowLimit?: number;
    reportVariant?: {
      tagId?: string;
    };
    embedOpts?: {
      embedId?: string;
      evalConnectionId?: string;
    };
  };
  /** Whether the schedule is currently suspended. */
  isSuspended: boolean;
  /** Unique identifier of the user who owns the schedule. */
  ownerId: string;
  /** Unique identifier of the user who last updated the schedule. */
  lastUpdatedByUser: string;
  /** Timestamp when the schedule was disabled, or null if the schedule is active. */
  disabledAt: string | null;
  /** Unique identifier of the member who disabled the schedule, or null if the schedule is active. */
  disabledBy: string | null;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
}

/** Response from {@link ReportsResource["updateSchedule"] | updateSchedule}. */
export interface UpdateReportScheduleResponse {
  /** Unique identifier of the scheduled export. */
  scheduledNotificationId: string;
  /** Unique identifier of the report. */
  reportId: string;
  schedule: {
    cronSpec: string;
    timezone: string;
  };
  configV2: {
    title: string;
    messageBody: string;
    notificationAttachments: {
      formatOptions:
        | {
            type: 'PDF';
            layout?: 'portrait' | 'landscape';
            scaleFactor?: number;
          }
        | {
            type: 'NONE';
          };
      workbookExportSource?: {
        type: 'report';
      };
    }[];
    includeLink?: boolean;
    runAsRecipient?: boolean;
    attachmentSettings?: {
      mergePdfAttachments?: boolean;
      zipAttachments?: boolean;
    };
    conditionOptions?:
      | {
          type: 'always';
        }
      | {
          type: 'workbook';
          conditions: (
            | {
                type: 'isEmpty';
                sheetId: string;
              }
            | {
                type: 'isNotEmpty';
                sheetId: string;
              }
            | {
                type: 'sheetColumn';
                sheetId: string;
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        }
      | {
          type: 'worksheet';
          conditions: (
            | {
                type: 'isEmpty';
              }
            | {
                type: 'isNotEmpty';
              }
            | {
                type: 'queryColumn';
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        }
      | {
          type: 'dashboard';
          conditions: (
            | {
                type: 'isEmpty';
                tileId: string;
              }
            | {
                type: 'isNotEmpty';
                tileId: string;
              }
            | {
                type: 'chartColumn';
                tileId: string;
                mode: 'any' | 'all';
                columnId: string;
                op:
                  | 'Equals'
                  | 'GreaterThan'
                  | 'GreaterThanEq'
                  | 'LessThan'
                  | 'LessThanEq'
                  | 'NotEquals'
                  | 'IsNotNull'
                  | 'IsNull'
                  | 'Contains'
                  | 'StartsWith'
                  | 'EndsWith';
                value?:
                  | {
                      type: 'null';
                    }
                  | {
                      type: 'bool';
                      val: boolean;
                    }
                  | {
                      type: 'number';
                      val: number;
                    }
                  | {
                      type: 'integer';
                      val: string;
                    }
                  | {
                      type: 'string';
                      val: string;
                    }
                  | {
                      type: 'datetime';
                      val: string;
                    }
                  | {
                      type: 'error';
                      val: string;
                    }
                  | {
                      type: 'record';
                      val: Record<string, unknown>;
                    }
                  | {
                      type: 'array';
                      val:
                        | {
                            type: 'boolArr';
                            data: (boolean | null)[];
                          }
                        | {
                            type: 'numberArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bigintArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'stringArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'datetimeArr';
                            data: (number | null)[];
                          }
                        | {
                            type: 'bytesArr';
                            data: (string | null)[];
                          }
                        | {
                            type: 'variantArr';
                            data: (string | null)[];
                          };
                    }
                  | {
                      type: 'variant';
                      val: string;
                    };
              }
          )[];
        };
    alertConfig?: {
      window: 'alltime' | 'daily' | 'weekly' | 'always';
      volume?: number | null;
    };
    notificationName?: string;
    reportSettings?: {
      filters?: {
        id: string;
        filterStr: string;
      }[];
      variableValues?: components['schemas']['DashVariables'];
    };
    dynamicExportSettings?: {
      variables?: Record<
        string,
        | {
            type: 'boolean';
          }
        | {
            type: 'number';
          }
        | {
            type: 'text';
          }
        | {
            type: 'date';
          }
        | {
            type: 'number-list';
          }
        | {
            type: 'text-list';
          }
        | {
            type: 'date-list';
          }
        | {
            type: 'boolean-list';
          }
        | {
            type: 'date-range';
          }
        | {
            type: 'number-range';
          }
        | {
            type: 'date-null';
          }
        | {
            type: 'number-null';
          }
        | {
            type: 'text-null';
          }
        | {
            type: 'boolean-null';
          }
      >;
    };
    dynamicRecipientSettings?: {
      sourceNodeId: string;
      recipientColumnId: string;
    };
    rowLimit?: number;
    reportVariant?: {
      tagId?: string;
    };
    embedOpts?: {
      embedId?: string;
      evalConnectionId?: string;
    };
  };
  /** Whether the schedule is currently suspended. */
  isSuspended: boolean;
  /** Unique identifier of the user who owns the schedule. */
  ownerId: string;
  /** Unique identifier of the user who last updated the schedule. */
  lastUpdatedByUser: string;
  /** Timestamp when the schedule was disabled, or null if the schedule is active. */
  disabledAt: string | null;
  /** Unique identifier of the member who disabled the schedule, or null if the schedule is active. */
  disabledBy: string | null;
  /** The identifier of the user who created this object. */
  createdBy: string;
  /** The identifier of the user or process that last updated this object. */
  updatedBy: string;
  /** When the object was created. */
  createdAt: string;
  /** When the object was last updated. */
  updatedAt: string;
}

/** Response from {@link ReportsResource["deleteSchedule"] | deleteSchedule}. */
export type DeleteReportScheduleResponse = Record<string, never>;

/** Response from {@link ReportsResource["send"] | send}. */
export type SendReportResponse = Record<string, never>;

/** Response from {@link ReportsResource["listSources"] | listSources}. */
export type ListReportSourcesResponse = (
  | {
      type: 'data-model';
      dataModelId: string;
      elementIds: string[];
      versionTagId?: string;
    }
  | {
      type: 'dataset';
      inodeId: string;
    }
  | {
      type: 'table';
      inodeId: string;
    }
  | {
      type: 'custom-sql';
      connectionId: string;
      definition: string;
      customSqlId: string;
    }
)[];

// ---------------------------------------------------------------------------
// Sub-resource accessors (hydration)
// ---------------------------------------------------------------------------

/** Sub-resource accessors available on hydrated report entries. */
export interface ReportSubResources {
  schedules: {
    /**
     * List scheduled report exports
     *
     * Source docs: https://help.sigmacomputing.com/reference/listreportschedules
     *
     * This endpoint retrieves a list of all scheduled exports for a specified report. These schedules represent planned events that can trigger exports based on various conditions and timings.
     *
     *   ### Usage Notes
     *   - The **reportId** in the path parameter identifies the report whose scheduled exports are to be listed. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
     *   - Use pagination query parameters to manage the volume of data returned by this endpoint.
     *
     *   ### Usage Scenarios
     *   - **Schedule overview**: Administrators or users with appropriate permissions can monitor and review all active scheduled exports associated with a report.
     *   - **Audit and compliance**: This functionality is useful for auditing purposes to ensure that all scheduled operations are configured correctly and comply with organizational policies.
     *
     *   ### Best Practices
     *   - Implement client-side handling to manage paginated data effectively, especially in cases where a report has a large number of scheduled exports.
     *   - Regularly audit schedules to keep them up-to-date and aligned with current workflows.
     *
     * @param query - Query parameters
     * @returns ListReportSchedulesResponse
     */
    list(
      query?: ListReportSchedulesQuery,
    ): Promise<DeepReadonly<ListReportSchedulesResponse>>;
    /**
     * List scheduled report exports (auto-paginate all pages)
     *
     * Source docs: https://help.sigmacomputing.com/reference/listreportschedules
     *
     * This endpoint retrieves a list of all scheduled exports for a specified report. These schedules represent planned events that can trigger exports based on various conditions and timings.
     *
     *   ### Usage Notes
     *   - The **reportId** in the path parameter identifies the report whose scheduled exports are to be listed. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
     *   - Use pagination query parameters to manage the volume of data returned by this endpoint.
     *
     *   ### Usage Scenarios
     *   - **Schedule overview**: Administrators or users with appropriate permissions can monitor and review all active scheduled exports associated with a report.
     *   - **Audit and compliance**: This functionality is useful for auditing purposes to ensure that all scheduled operations are configured correctly and comply with organizational policies.
     *
     *   ### Best Practices
     *   - Implement client-side handling to manage paginated data effectively, especially in cases where a report has a large number of scheduled exports.
     *   - Regularly audit schedules to keep them up-to-date and aligned with current workflows.
     *
     * @param query - Query parameters
     * @yields ListReportSchedulesResponse
     */
    listAll(
      query?: ListReportSchedulesQuery,
    ): PaginatedAsyncGenerator<DeepReadonly<ListReportSchedulesResponse>>;
    /**
     * Add report schedule
     *
     * Source docs: https://help.sigmacomputing.com/reference/createreportschedule
     *
     * This endpoint allows you to create a scheduled export of a report. Scheduled exports are triggered by specific conditions or timings.
     *
     *   ### Usage notes
     *   - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
     *   - Retrieve relevant IDs based on the export contents and destination:
     *     - To export to one or more Sigma users, retrieve the **memberId** by calling the [/v2/members](listmembers) endpoint.
     *     - To export to one or more Sigma teams, retrieve the **teamId** by calling the [/v2/teams](listteams) endpoint.
     *   - Reports can only be exported as entire PDFs. Exporting a specific page or element is not supported.
     *   - If you want to impersonate a user when scheduling a report export, use the [/v2/auth/token](posttoken) endpoint.
     *
     *   ### Usage scenarios
     *   - **Periodic reporting**: Use this endpoint to trigger routine report exports at specific intervals (such as weekly or monthly) or when specific business conditions are met (such as specific sales targets or usage thresholds).
     *
     *   ### Best practices
     *   - Regularly review and audit schedules to ensure unnecessary or expensive schedules are not being added.
     *
     * @param body - The request body.
     * @returns CreateReportScheduleResponse
     */
    create(
      body: CreateReportScheduleBody,
    ): Promise<DeepReadonly<CreateReportScheduleResponse>>;
  };
  sources: {
    /**
     * List report sources
     *
     * Source docs: https://help.sigmacomputing.com/reference/listreportsources
     *
     * Get the data sources of a report given the reportId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
     *
     * ### Usage notes
     * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
     *
     * ### Best practices
     * - Validate the **reportId** on the client side before making a request to avoid unnecessary server load caused by invalid requests.
     * @returns ListReportSourcesResponse
     */
    list(): Promise<DeepReadonly<ListReportSourcesResponse>>;
  };
  /**
   * Duplicate a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/copyreport
   *
   * This endpoint lets you duplicate an existing report. The report copy can be placed in a different folder from the original report, potentially with a different name and description.
   *
   * ### Usage notes
   * - The `reportId` parameter specifies the original report to be copied. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - The `destinationFolderId` in the request body specifies where to place the new report.
   *   - To use the "My Documents" folder for a specific user as the destination folder, call the [/v2/members/{memberId}](getmember) endpoint and use the `homeFolderId` included in the response.
   *   - Retrieve the **folderId** of all folders by calling the [/v2/files](fileslist) endpoint and reviewing the `id` field in the response for files with a `type` of `folder`.
   *
   * ### Usage scenarios
   * - **Reuse report structure**: Use an existing report as a baseline to make changes to.
   * - **Backup**: Make a backup of a report before making significant changes.
   *
   * ### Best practices
   * - Always verify that the destination folder has the correct permissions set to avoid access issues.
   *
   * @param body - The request body.
   * @returns CopyReportResponse
   */
  copy(body: CopyReportBody): Promise<DeepReadonly<CopyReportResponse>>;
  /**
   * Export data from a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/exportreport
   *
   * This endpoint lets you create a file containing data exported from a report. Reports are exported as entire PDF documents.
   *
   * Successful requests return a **queryId**. Use the **queryId** with the [/v2/query/{queryId}/download](downloadquery) endpoint to download the file when it is ready. See [Download an exported file](downloadquery).
   *
   * To export a report to a specific destination or recipient, see the [/v2/reports/{reportId}/send](sendreport) endpoint.
   *
   * ### Usage notes
   * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - Reports are exported as entire documents. You cannot export a specific page or element.
   * - Only PDF exports are supported. Use the `layout` parameter to specify either `portrait` or `landscape` layout.
   * - This endpoint is rate limited to 400 requests per minute.
   * - For security and privacy reasons, the `queryId` used to download the exported file expires 1 hour after your request is received. You can extend this to 6 hours using the `resultsValidityTimeMs` parameter.
   *
   * ### Usage scenarios
   * - **Conditional report export**: Based on an external set of conditions, export a report from Sigma.
   * - **Integration**: Integrate enriched and analyzed data with other applications, providing the necessary data in a format that these applications can process.
   *
   * ### Best practices
   * - Clearly specify parameters to retrieve only the necessary data, optimizing performance and reducing network load.
   *
   * @param body - The request body.
   * @returns ExportReportResponse
   */
  export(body: ExportReportBody): Promise<DeepReadonly<ExportReportResponse>>;
  /**
   * Export a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/sendreport
   *
   * Export a report to one or more specified destinations or recipients. Supported destinations include Slack channels, Google Drive, cloud storage, and webhooks. Supported recipients include an organization member, team, or email address.
   *
   * If you want to download a report PDF instead, see the [/v2/reports/{reportId}/export](exportreport) endpoint.
   *
   * ### Usage notes
   * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - Only PDF exports are supported for reports.
   * - This endpoint is rate limited to 100 requests per minute.
   *
   * @param body - The request body.
   * @returns SendReportResponse
   */
  send(body: SendReportBody): Promise<DeepReadonly<SendReportResponse>>;
}

/** A report entry with sub-resource accessor methods. Response data properties are deeply readonly. */
export type HydratedReport<T> = DeepReadonly<T> & ReportSubResources;

// ---------------------------------------------------------------------------
// ReportsResource
// ---------------------------------------------------------------------------

/** @internal Attach sub-resource accessors to a report entry. */
function _hydrateReport<T extends object>(
  entry: T,
  resource: ReportsResource,
): HydratedReport<T> {
  return Object.assign(entry, {
    schedules: {
      list: (query?: ListReportSchedulesQuery) =>
        resource.listSchedules((entry as any).reportId, query),
      listAll: (query?: ListReportSchedulesQuery) =>
        resource.listSchedulesAll((entry as any).reportId, query),
      create: (body: CreateReportScheduleBody) =>
        resource.createSchedule((entry as any).reportId, body),
    },
    sources: {
      list: () => resource.listSources((entry as any).reportId),
    },
    copy: (body: CopyReportBody) =>
      resource.copy((entry as any).reportId, body),
    export: (body: ExportReportBody) =>
      resource.export((entry as any).reportId, body),
    send: (body: SendReportBody) =>
      resource.send((entry as any).reportId, body),
  }) as HydratedReport<T>;
}

export class ReportsResource {
  constructor(private client: Client<paths>) {}

  /**
   * List reports
   *
   * Source docs: https://help.sigmacomputing.com/reference/listreports
   *
   * This endpoint retrieves a list of all available reports.
   *
   * Available reports include any reports in your My Documents folder and any reports you have access to.
   *
   * Users with the Admin account type can optionally retrieve all reports in the organization.
   *
   * ### Usage notes
   * This endpoint requires no parameters for basic requests, but supports query parameters for pagination and response limit.
   *
   * ### Pagination
   *
   * This endpoint supports pagination, which lets you retrieve large sets of data in manageable segments. The response includes pagination details as follows:
   *
   * - hasMore: A boolean value indicating whether there are more pages of data available beyond the current page.
   * - total: The total number of entries available across all pages.
   * - nextPage: An identifier or token that you can use in a subsequent request to retrieve the next page of data.
   *
   * #### Example response for pagination
   * ```json
   * {
   *   "hasMore": true,
   *   "total": 104,
   *   "nextPage": "50"
   * }
   * ```
   *
   * To request additional pages, include the `nextPage` option in your next request as the value of the `page` option. Repeat this process until `nextPage` returns `null`, indicating that there are no more pages to return.
   *
   * ### Usage scenarios
   * - **Report navigation**: Allows users to view their collection of reports and locate ones they need.
   * - **Integration points**: Useful for building integrations that need to present users with a list of their available reports, such as in custom applications using embeds.
   *
   * @param query - Query parameters
   * @returns ListReportsResponse
   */
  async list(
    query?: ListReportsQuery,
  ): Promise<HydratePage<ListReportsResponse, ReportSubResources>> {
    const { data, error, response } = await this.client.GET('/v2/reports', {
      params: { query },
    });
    if (error) throw new SigmaApiError(error, response);
    const result = data! as any;
    if (Array.isArray(result)) {
      return result.map((e: any) => _hydrateReport(e, this)) as any;
    }
    if (result.entries && Array.isArray(result.entries)) {
      result.entries = result.entries.map((e: any) => _hydrateReport(e, this));
    }
    return result;
  }

  /**
   * List reports (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listreports
   *
   * This endpoint retrieves a list of all available reports.
   *
   * Available reports include any reports in your My Documents folder and any reports you have access to.
   *
   * Users with the Admin account type can optionally retrieve all reports in the organization.
   *
   * ### Usage notes
   * This endpoint requires no parameters for basic requests, but supports query parameters for pagination and response limit.
   *
   * ### Pagination
   *
   * This endpoint supports pagination, which lets you retrieve large sets of data in manageable segments. The response includes pagination details as follows:
   *
   * - hasMore: A boolean value indicating whether there are more pages of data available beyond the current page.
   * - total: The total number of entries available across all pages.
   * - nextPage: An identifier or token that you can use in a subsequent request to retrieve the next page of data.
   *
   * #### Example response for pagination
   * ```json
   * {
   *   "hasMore": true,
   *   "total": 104,
   *   "nextPage": "50"
   * }
   * ```
   *
   * To request additional pages, include the `nextPage` option in your next request as the value of the `page` option. Repeat this process until `nextPage` returns `null`, indicating that there are no more pages to return.
   *
   * ### Usage scenarios
   * - **Report navigation**: Allows users to view their collection of reports and locate ones they need.
   * - **Integration points**: Useful for building integrations that need to present users with a list of their available reports, such as in custom applications using embeds.
   *
   * @param query - Query parameters
   * @yields ListReportsResponse
   */
  listAll(
    query?: ListReportsQuery,
  ): PaginatedAsyncGenerator<
    HydratePage<ListReportsResponse, ReportSubResources>
  > {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.list({
          ...query,
          page: cursor,
        } as ListReportsQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Create a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/createreport
   *
   * This endpoint lets you create an empty report in Sigma, enabling you to build presentation-ready documents for sharing insights with stakeholders.
   *
   * ### Usage notes
   * - The **name** parameter is required to provide a name for the new report.
   * - Use the **folderId** to specify the folder in which to save the report. Retrieve the **folderId** by calling the [/v2/files](fileslist) endpoint and reviewing the `id` field in the response for files with a `type` of `folder`.
   *
   * ### Usage scenarios
   * - **Reliable export formatting**: Users can quickly generate a new blank report to create predictable, paginated exports.
   *
   * ### Best practices
   * - **Naming conventions**: Establish and follow consistent naming conventions for reports to make it easier to manage and identify them within larger projects.
   * - **Folder organization**: Use the **folderId** to organize reports into relevant folders, which helps in maintaining a tidy workspace, especially in environments with multiple users or teams.
   * - **Access control**: Regularly review and manage access permissions for new reports, ensuring that only the appropriate personnel can view or edit sensitive data.
   *
   * @param body - The request body.
   * @returns CreateReportResponse
   */
  async create(
    body: CreateReportBody,
  ): Promise<HydratedReport<CreateReportResponse>> {
    const { data, error, response } = await this.client.POST('/v2/reports', {
      body,
    });
    if (error) throw new SigmaApiError(error, response);
    return _hydrateReport(data! as any, this);
  }

  /**
   * Get a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/getreport
   *
   * This endpoint retrieves a report by its unique identifier (`reportId`). It provides detailed information about the report, including its name, URL, path, and other metadata.
   *
   * ### Usage notes
   * - The **reportId** parameter must be a valid UUID that uniquely identifies the report. Invalid or nonexistent IDs return an error. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *
   * ### Usage scenarios
   * - **Report search**: Allows users to view all of their reports and locate a specific one, as well as filter by criteria such as if it is archived.
   * - **Data retrieval**: Developers can use this endpoint to programmatically retrieve details about a specific report to display its content or metadata in a custom user interface.
   * - **Integration**: Use this endpoint for integrations where other systems need to fetch report details based on an ID provided through another interface or workflow.
   *
   * ### Best practices
   * - Validate the **reportId** on the client side before making a request to avoid unnecessary server load caused by invalid requests.
   *
   * @param reportId - Unique identifier of the report.
   * @returns GetReportResponse
   */
  async get(reportId: string): Promise<HydratedReport<GetReportResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/reports/{reportId}',
      { params: { path: { reportId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return _hydrateReport(data! as any, this);
  }

  /**
   * Duplicate a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/copyreport
   *
   * This endpoint lets you duplicate an existing report. The report copy can be placed in a different folder from the original report, potentially with a different name and description.
   *
   * ### Usage notes
   * - The `reportId` parameter specifies the original report to be copied. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - The `destinationFolderId` in the request body specifies where to place the new report.
   *   - To use the "My Documents" folder for a specific user as the destination folder, call the [/v2/members/{memberId}](getmember) endpoint and use the `homeFolderId` included in the response.
   *   - Retrieve the **folderId** of all folders by calling the [/v2/files](fileslist) endpoint and reviewing the `id` field in the response for files with a `type` of `folder`.
   *
   * ### Usage scenarios
   * - **Reuse report structure**: Use an existing report as a baseline to make changes to.
   * - **Backup**: Make a backup of a report before making significant changes.
   *
   * ### Best practices
   * - Always verify that the destination folder has the correct permissions set to avoid access issues.
   *
   * @param reportId - Unique identifier of the report.
   * @param body - The request body.
   * @returns CopyReportResponse
   */
  async copy(
    reportId: string,
    body: CopyReportBody,
  ): Promise<DeepReadonly<CopyReportResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/reports/{reportId}/copy',
      { params: { path: { reportId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Export data from a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/exportreport
   *
   * This endpoint lets you create a file containing data exported from a report. Reports are exported as entire PDF documents.
   *
   * Successful requests return a **queryId**. Use the **queryId** with the [/v2/query/{queryId}/download](downloadquery) endpoint to download the file when it is ready. See [Download an exported file](downloadquery).
   *
   * To export a report to a specific destination or recipient, see the [/v2/reports/{reportId}/send](sendreport) endpoint.
   *
   * ### Usage notes
   * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - Reports are exported as entire documents. You cannot export a specific page or element.
   * - Only PDF exports are supported. Use the `layout` parameter to specify either `portrait` or `landscape` layout.
   * - This endpoint is rate limited to 400 requests per minute.
   * - For security and privacy reasons, the `queryId` used to download the exported file expires 1 hour after your request is received. You can extend this to 6 hours using the `resultsValidityTimeMs` parameter.
   *
   * ### Usage scenarios
   * - **Conditional report export**: Based on an external set of conditions, export a report from Sigma.
   * - **Integration**: Integrate enriched and analyzed data with other applications, providing the necessary data in a format that these applications can process.
   *
   * ### Best practices
   * - Clearly specify parameters to retrieve only the necessary data, optimizing performance and reducing network load.
   *
   * @param reportId - Unique identifier of the report.
   * @param body - The request body.
   * @returns ExportReportResponse
   */
  async export(
    reportId: string,
    body: ExportReportBody,
  ): Promise<DeepReadonly<ExportReportResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/reports/{reportId}/export',
      { params: { path: { reportId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List scheduled report exports
   *
   * Source docs: https://help.sigmacomputing.com/reference/listreportschedules
   *
   * This endpoint retrieves a list of all scheduled exports for a specified report. These schedules represent planned events that can trigger exports based on various conditions and timings.
   *
   *   ### Usage Notes
   *   - The **reportId** in the path parameter identifies the report whose scheduled exports are to be listed. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *   - Use pagination query parameters to manage the volume of data returned by this endpoint.
   *
   *   ### Usage Scenarios
   *   - **Schedule overview**: Administrators or users with appropriate permissions can monitor and review all active scheduled exports associated with a report.
   *   - **Audit and compliance**: This functionality is useful for auditing purposes to ensure that all scheduled operations are configured correctly and comply with organizational policies.
   *
   *   ### Best Practices
   *   - Implement client-side handling to manage paginated data effectively, especially in cases where a report has a large number of scheduled exports.
   *   - Regularly audit schedules to keep them up-to-date and aligned with current workflows.
   *
   * @param reportId - Unique identifier of the report.
   * @param query - Query parameters
   * @returns ListReportSchedulesResponse
   */
  async listSchedules(
    reportId: string,
    query?: ListReportSchedulesQuery,
  ): Promise<DeepReadonly<ListReportSchedulesResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/reports/{reportId}/schedules',
      { params: { path: { reportId }, query } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List scheduled report exports (auto-paginate all pages)
   *
   * Source docs: https://help.sigmacomputing.com/reference/listreportschedules
   *
   * This endpoint retrieves a list of all scheduled exports for a specified report. These schedules represent planned events that can trigger exports based on various conditions and timings.
   *
   *   ### Usage Notes
   *   - The **reportId** in the path parameter identifies the report whose scheduled exports are to be listed. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *   - Use pagination query parameters to manage the volume of data returned by this endpoint.
   *
   *   ### Usage Scenarios
   *   - **Schedule overview**: Administrators or users with appropriate permissions can monitor and review all active scheduled exports associated with a report.
   *   - **Audit and compliance**: This functionality is useful for auditing purposes to ensure that all scheduled operations are configured correctly and comply with organizational policies.
   *
   *   ### Best Practices
   *   - Implement client-side handling to manage paginated data effectively, especially in cases where a report has a large number of scheduled exports.
   *   - Regularly audit schedules to keep them up-to-date and aligned with current workflows.
   *
   * @param reportId - Unique identifier of the report.
   * @param query - Query parameters
   * @yields ListReportSchedulesResponse
   */
  listSchedulesAll(
    reportId: string,
    query?: ListReportSchedulesQuery,
  ): PaginatedAsyncGenerator<DeepReadonly<ListReportSchedulesResponse>> {
    // eslint-disable-next-line @typescript-eslint/no-this-alias
    const resource = this;
    async function* gen() {
      let cursor: string | undefined;
      do {
        const page = await resource.listSchedules(reportId, {
          ...query,
          page: cursor,
        } as ListReportSchedulesQuery);
        yield page;
        cursor = (page as { nextPage?: string | null })['nextPage'] as
          string | undefined;
      } while (cursor);
    }
    return new PaginatedAsyncGenerator(gen());
  }

  /**
   * Add report schedule
   *
   * Source docs: https://help.sigmacomputing.com/reference/createreportschedule
   *
   * This endpoint allows you to create a scheduled export of a report. Scheduled exports are triggered by specific conditions or timings.
   *
   *   ### Usage notes
   *   - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *   - Retrieve relevant IDs based on the export contents and destination:
   *     - To export to one or more Sigma users, retrieve the **memberId** by calling the [/v2/members](listmembers) endpoint.
   *     - To export to one or more Sigma teams, retrieve the **teamId** by calling the [/v2/teams](listteams) endpoint.
   *   - Reports can only be exported as entire PDFs. Exporting a specific page or element is not supported.
   *   - If you want to impersonate a user when scheduling a report export, use the [/v2/auth/token](posttoken) endpoint.
   *
   *   ### Usage scenarios
   *   - **Periodic reporting**: Use this endpoint to trigger routine report exports at specific intervals (such as weekly or monthly) or when specific business conditions are met (such as specific sales targets or usage thresholds).
   *
   *   ### Best practices
   *   - Regularly review and audit schedules to ensure unnecessary or expensive schedules are not being added.
   *
   * @param reportId - Unique identifier of the report.
   * @param body - The request body.
   * @returns CreateReportScheduleResponse
   */
  async createSchedule(
    reportId: string,
    body: CreateReportScheduleBody,
  ): Promise<DeepReadonly<CreateReportScheduleResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/reports/{reportId}/schedules',
      { params: { path: { reportId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Update a report schedule
   *
   * Source docs: https://help.sigmacomputing.com/reference/updatereportschedule
   *
   * This endpoint allows you to update an existing scheduled report export.
   *
   *   ### Usage notes
   *   - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *   - Retrieve the **scheduleId** by calling the [/v2/reports/{reportId}/schedules](listreportschedules) endpoint.
   *
   *   ### Usage scenarios
   *   - **Audit schedules**: Update schedules to ensure they are being exported at the correct times or to the correct recipients.
   *
   * @param reportId - Unique identifier of the report.
   * @param scheduleId - Unique identifier of the schedule.
   * @param body - The request body.
   * @returns UpdateReportScheduleResponse
   */
  async updateSchedule(
    reportId: string,
    scheduleId: string,
    body: UpdateReportScheduleBody,
  ): Promise<DeepReadonly<UpdateReportScheduleResponse>> {
    const { data, error, response } = await this.client.PATCH(
      '/v2/reports/{reportId}/schedules/{scheduleId}',
      { params: { path: { reportId, scheduleId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Delete a scheduled export for a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/deletereportschedule
   *
   * This endpoint deletes a specific scheduled export from a report. Deleting a schedule prevents any previously configured exports or alerts from being triggered at the scheduled times.
   *
   *   ### Usage notes
   *   - The `reportId` in the path is required to identify the report. Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *   - The `scheduleId` in the path is required to specify which schedule to delete. Retrieve the **scheduleId** by calling the [/v2/reports/{reportId}/schedules](listreportschedules) endpoint and using the `scheduledNotificationId` in the response.
   *
   *   ### Usage scenarios
   *   - **Audit operations**: Use this endpoint to remove unnecessary or outdated schedules that are no longer relevant to the report's current use case.
   *
   *   ### Best practices
   *   - Make sure that deleting a given schedule is in compliance with your organization's data governance and operational policies.
   *   - Verify the `scheduleId` and `reportId` before making a deletion request to prevent errors.
   *
   * @param reportId - Unique identifier of the report.
   * @param scheduleId - Unique identifier of the schedule.
   * @returns DeleteReportScheduleResponse
   */
  async deleteSchedule(
    reportId: string,
    scheduleId: string,
  ): Promise<DeepReadonly<DeleteReportScheduleResponse>> {
    const { data, error, response } = await this.client.DELETE(
      '/v2/reports/{reportId}/schedules/{scheduleId}',
      { params: { path: { reportId, scheduleId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * Export a report
   *
   * Source docs: https://help.sigmacomputing.com/reference/sendreport
   *
   * Export a report to one or more specified destinations or recipients. Supported destinations include Slack channels, Google Drive, cloud storage, and webhooks. Supported recipients include an organization member, team, or email address.
   *
   * If you want to download a report PDF instead, see the [/v2/reports/{reportId}/export](exportreport) endpoint.
   *
   * ### Usage notes
   * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   * - Only PDF exports are supported for reports.
   * - This endpoint is rate limited to 100 requests per minute.
   *
   * @param reportId - Unique identifier of the report.
   * @param body - The request body.
   * @returns SendReportResponse
   */
  async send(
    reportId: string,
    body: SendReportBody,
  ): Promise<DeepReadonly<SendReportResponse>> {
    const { data, error, response } = await this.client.POST(
      '/v2/reports/{reportId}/send',
      { params: { path: { reportId } }, body },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }

  /**
   * List report sources
   *
   * Source docs: https://help.sigmacomputing.com/reference/listreportsources
   *
   * Get the data sources of a report given the reportId. The response can be a dataset (deprecated), table, custom SQL element, or the data model document and specific elements used as a source.
   *
   * ### Usage notes
   * - Retrieve the **reportId** by calling the [/v2/reports](listreports) endpoint.
   *
   * ### Best practices
   * - Validate the **reportId** on the client side before making a request to avoid unnecessary server load caused by invalid requests.
   *
   * @param reportId - Unique identifier of the report.
   * @returns ListReportSourcesResponse
   */
  async listSources(
    reportId: string,
  ): Promise<DeepReadonly<ListReportSourcesResponse>> {
    const { data, error, response } = await this.client.GET(
      '/v2/reports/{reportId}/sources',
      { params: { path: { reportId } } },
    );
    if (error) throw new SigmaApiError(error, response);
    return data!;
  }
}
